# Pro traceability investigation archive

This folder keeps the downloaded Pro investigation unchanged. The ZIP and extracted files have matching SHA-256 checksums. The extracted bundle has 31 files. Its patch, scripts, and experimental source have **not** been applied to the package or run from this folder.

Start with the three documents under `exploreda-investigation/deliverables/docs/intent/deterministic-rendering-and-data-traceability/`. The source and checks under `exploreda-investigation/` are evidence for an isolated experiment, not proof of live explorEDA behavior. Pro could not run `pnpm check` or its supplied Vitest test.

The earlier standalone 12-example HTML prototype is not in this ZIP and was not present in Downloads during this import. The Software Design Sleuth artifacts in `docs/intent/deterministic-rendering-and-data-traceability/` use the supplied conversation view for that prototype's product ideas. They do not claim to verify its code or diagnostics.

Imported on `main` without a commit. The original download remains in `/Users/byronwall/Downloads/exploreda-deterministic-rendering-data-traceability.zip`.
