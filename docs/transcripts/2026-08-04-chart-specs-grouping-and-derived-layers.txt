All right, here we go. This note is going to continue yesterday's discussion on interactive database, in particular the idea of creating a spec that defines the charts. And the dilemma yesterday that I'll keep going on is when we've got a chart where we want to color. So for example, we've got points, we scatter chart. Got a hundred of them, X and Y, but they belong to three different groups. And kind of the first thing you do is you'll color by those three groups, A, B, C. But then what often happens is you see oh ABC actually looks like they're forming some sort of relationship between Y and X. And so now you want to add a regression line or a smooth line or some sort of line. You want the line to go through points as though they are a small isolated on their own subset of data. Alright, and so big question is how to express the group. And if you have a good way of expressing the group when switch over to it first when to keep a simpler relationship where you just say color is a parameter driven by this variable. And so one option is you could always just create the group internally. But it's kind of a little bit wasteful to create the group if you're not going to do any group stuff with it. Bad answer if we're just trying to figure out how to do color because we don't want the complexity of needing to create or make sense of some outer container just to find a variable in for color. But then once you get to wanting to draw lines for stuff, now it's a must. You must create the groups somehow. Because otherwise you're gonna make a mistake and you gotta draw your regression lines. And so clearly you need you need to have the data group so you can create the regression lines. If you wanted regression lines, you checked the box for regression lines. And then internally, inside the scatter plot, I took care of all the math for rendering regression lines, which means the scatter chart is now aware of all this grouping, subsetting, logic, computing regression lines. It's then sampling those lines to draw the final regression line. And then it had additional logic to keep track of all the regression parameters. And it was correct. And so all of that works, but the problem with it is I may want to do regression lines somewhere else. Is that true? I don't know, maybe. Just draw points. I want a line renderer. You could go draw the regression line. All right, what else we have here? Well, maybe our regression line has a some sort of confidence interval on it. So we might want to have a shaded region as well. All right, with that idea. And it's just responsible for drawing line confidence intervals. And where else might that be useful? Maybe I've got an existing chart and I just want to show a trend line on it. I can see that being helpful. But if I go that route, and now regression lines is some dedicated thing. Now I'm back into the world of my scatter chart, doesn't really need to be aware of grouping. I'm coming over the top with some additional glyphs, which include the lines, the confidence areas, confidence intervals, all of that stuff. But if I'm doing that now, I'm needing to render the scatter chart, render its points, render their colors, colors driven by group assignment. I've separately indicated that I want to compute these regression lines. And so now I'm choosing to plot the regression lines in that chart. Maybe when I'm defining the regression, this is where I'm describing the split to be done. You saw the question of whether you've actually split the tables or just create one big table with moles. I think again you want to actually split the table. So you've got small inspectable tables where the user can go see what went in for the regression and what the predicted result is. But then when we go to render that thing, is it enough to say I want to just render that regression line? It's like now I've just got this extra object floating around. And now I'm just choosing to render bliss for them. So would that work? Basically the consequence of that is progression lines are computed outside. It's just a thing that is available. And then you've got to tell the plot and bring that in and plot it. So I think that works. But what were some of the other reasons we wanted to group stuff? Are they all they all collapse to the same idea? Which is that we should just do the group things outside of the charts. Another idea is that you want to plot like the ellipsoidal densities, the regions of expectation. I've got some group, I want to go plot the density contours. And I think that one works. Apply some logic, and then hold on to those results. I'm splitting them into four groups. The groups are A, B, C, D. I'm then computing a density distribution. And that is valid for that subgroup. So then I guess the unknown is how do you how do you show a UI for that? You've got technically this density thing is some table. But way more likely is that you want to treat it as like an object. This is a density calculation. Now we can go plot it. And so then I'm choosing, well, is that alright? Because in Shifty, when I was doing density calculations, again, all the parameters were in the chart settings. And so if you need to go change the bandwidth or the number of contours to plot, those would just be the settings in the scattered plot settings. Well I've got I I could set the bandwidth when I do my this kind of split and apply operation. And so if I wanted to go override one of the four if I were doing it manually I could surely go do that but then often the point where you're wanting to change the density uh bandwidth is once it's rendered you can see that it's not what you want you really almost just want to have a slider so we can go tweak it on the spot and get the tightest feedback between here's how that bandwidth parameter controls that density because I'm just confirming the visual but now we've got a little bit of a dilemma because if we want the density calculation to be on the side I'm now having to figure out how to give that thing some UI in the chart settings. And maybe this isn't so hard to think about like we've got this chart that's made up of scatter points probably we need to render some sort of density and so it's easy to imagine that our chart just includes settings info for the things that are relevant to it. And so we've got settings to control point size color maybe. And then since we've opted to render these distributions, we then have settings for those as well. And then I guess where this is added, is on something like density. You could realistically render four densities in one chart. In the same way that you might choose to render four regression lines in one chart. Or you might choose to facet, and now you're wanting to have one regression line per chart, maybe, or one density per chart, maybe. But then, okay, let's do the crazier scenario, which is that you faceted, but you still need a regression line per colored line. So I'm doing my main pressure first engine speed. I've fastened by range. It is I've got nine ranges. It's like inside there I've got nine times five, forty five regression lines that mean computed. And you look at it like that, now we're back in the world of I really don't want to be computing my regressions outside of the facet. Because now I'm gonna have to keep my chart faceting and my regression synchronized. I'm sure it's gonna make a mistake. And so this again is pointing at I really want a simplified version of the chart definition is describing the regression it's going to render. And maybe this is just a shortcut. So we get good default behavior. And maybe behind the scenes. Now we're back into having this tight coupling between chart definitions and transformations and other structure. And maybe that's just how it has to work. It's like if you want really smooth, clean, easy configuration that just works, then really it is the chart that knows what needs to be created. And so the chart can go and describe all that. And separately. layers of expectable specs because we've clearly got some raw spec. Oh we've got some set of settings that the user is picking to go make the dashboards. We've got some set of here's all the sources, calculations, etc that are needed to actually do that. And then we've got once in all gets put together and then you've got the spec for and here's how you actually convert those things into glyphs and that last one okay so you need that last one if there actually a difference or is it that we've really got kind of one chart spec that includes sources, transforms, regressions, etc. And then inside it are blocks that can be expanded so that the user can override specific settings if they want to. Maybe that's how it works. Alright, we'll leave it there for now. I think this one still needs some more discussion.