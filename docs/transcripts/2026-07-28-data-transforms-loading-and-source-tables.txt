All right, here we go. We're going to continue taking notes, dictating ideas around data visualization systems, in particular interactive data visualization systems. And what's the surface area covered so far. Talked a bit about how source data moves through the system with some number of transformations, combinations, calculations, and that ultimately that arrives at set of rows and columns that need to be visualized. We then feed that into a chart primitive, but that our chart primitive can take several forms depending on exactly how we're building this thing and the simple system. Simple in the sense of we're encoding kind of our dashboard semantics just into the charts. But really, we've encoded something that's got a little bit more content to it because it's taking source data, applying a transformation, and then rendering glyphs based off of that transformed data. And so if the user came through and wanted to use that system to just make an arbitrary bar chart or row chart. They're now applying some hideous workarounds to basically reverse the internal transformation before it comes through. And so then a better way of doing that is to split the data side of things from the rendering side of things. And that life is easy on both sides. You can take all your source tables, do all your mappings, arrive at some really nice internal tables that have exactly the columns you want to go plot. You can then go and build out the visualization side of things so that it can take some really dumb simple incoming tables and render the glyphs. And then for our example of I've got a dashboard, I want to have a row chart that's aggregating and counting keys. But then if what you really want to build is something that is user configurable, well now you're kind of back into this world of if I know I'm building a dashboard and I know I've got row charts and I know I want to present them as this aggregate count thing. Well, somehow I need to go as a developer build that in advance so that it's just available and so that the necessary transformation gets coupled with the visualization, and then the user's not required to go and manually create a transformation node and manually wire up a transformation node to a row chart. Um because there is a really smooth, clean, and desirable experience when you're looking at the kind of pre-configured dashboard primitives, which is you can create charts very quickly, and as long as the defaults are uh useful, the experience is quite pleasant. And so the previous note kind of dug into what are some of the various uh configuration-related details. In particular, I was talking a bit about the idea of filtering. So if you're in an interactive data visualization setting, and it's something of a dashboard where you're trying to do exploratory data analysis, you're trying to filter, dig in, and make sense of uh some trend you see. How does that work? And so with that added was kind of an additional layer of primitives that you'd really like to have access to as you're building out the experience for this thing. One layer of primitives is the idea of being able to somehow control the source data. So if you're going to be doing some amount of visualization in the front end, it makes it very good and responsive if you've got data on hand so that you could do client-side filtering. But in large data sets, there's usually a tremendous cost to actually sending that data down the wire. And so I was able to really uh cheat when I was building Shifty because it would do all the data over local host. And so you could often ignore the fact that you're sending 10, 20, 30 megabytes of data just to generate visualization. But some part of what it was doing in this 1020-30 megabytes of data is it was making available in advance all of the possible things that a user might go filter or interact with or want to chart. And that's fine, but it's uh extreme non-optimization to do it that way. I had at some point introduced uh a different mode where it would compute in advance the fields that were necessary and only send those to the client. And so this worked fine as long as you were just interacting with that subset of data. And you could still uh I guess there's two two dimensions there, two two two knobs to go term. One is am I sending all columns of data? That's a pretty easy one to you can go look at the data that's being requested for the visuals and just say's uh some calculation graph, here's the fields I know I need, uh here's the dependencies, I need that field for this, this, this, this. Go compute which columns you need. You might then arrive at uh I need eight of the possible 92 columns, and so then you send a request off to the server. Please give me eight, these exact eight columns. All I need, and it will just send all that data. But the additional parameter to go change is do you need all rows of data also? Because in a lot of cases, you might then immediately change the view so that you're only looking at a subset of data, and maybe you just sent a hundred thousand records over, but the user is immediately reducing that to 10,000 records and never looked at the other 90,000. Um maybe you could determine that in advance, and so this gives you another knob to go turn, which is well, just send me the 10,000 rows instead of the full 100. And then I can make that work. And so that's all uh starting to feel pretty good from a let's not send all the data point of view. And I've got this added wrinkle, which is that when the user wants to see something new, a how do you let them know what fields are available? Alright, that's pretty doable. B, how do you go incrementally request that data so that it comes to the front end in a way that can be incorporated accurately, honestly, without resending all the data. But given that I could only work on so much at a given time, I never went after these sorts of incremental data loading concerns with Shifty. But just sort of talking through the pipeline now. We've got source data, we've got some amounts of transformations to give the columns and fields that we want. We've got some amount of filtering. That filtering might apply to the raw source data. This was one of the explicit problems that Shifty had, had this idea of client-side calculations, which was calculations that only existed on the client because it was using information that maybe only the client knew, or calculations that were just easier to do on the front. Alright, so we got uh so many rows we can send, so many columns we can send. One other wrinkle that Shifty, I guess, had to deal with, sort of, was this idea that some columns exist in different tables, but there's relationships and there's a uh cardinality to it all. So primary data structures I kept track of the uh files, and you could have metadata attached to a file. Every file would have events that were found. Events could be determined from multiple event finding scripts, and so the event finder had an ID. The actual event that was found was linked to the event finder and had its own unique ID. So those were uniquely keyed by file ID, event finder ID, and that gave you a unique final event ID. Um then for every event that was found, you could run for every event that was found, you could then go run or more analyses against that event. An event was characterized by its start time and end time. So you would go run the analysis on that start time and end time. The analysis had its own analysis script. It'd go do a bunch of calculations, it would save all of that data. I was saving it in a JSON blob, but you can imagine actually materializing that into real tables. Probably should have. Now for any given analysis, it can only be run one time. So if you chose to run analysis one, maybe that was basic info against the found event. What was the uh shift duration? So the difference between in time and start time on the shift itself. So it's an event sound event level piece of data. And what was the uh max temperature or max engine speed maybe make a lot of uh duration versus max engine speed to demonstrate that longer shifts flared on engine speed, and maybe that's the problem, but the problem was resolved when you went from one set of hardware to another set of hardware, and so you'd split that chart on color using this hardware variable, which was a piece of file level metadata. And so this was a very, very, very, very common scenario where you're blending together these three or four sources of data. Uh just to round up the suite, uh then file you had file metadata. You also technically had uh found event metadata and then it's a net. And so then I used a real simple naming scheme to sort of differentiate where these variables are being sourced from. But you can imagine needing a more robust uh system of this variable is sourced through this path, and so now in order to execute it, I need to go run these joins. I need to execute this specific specific SQL to get my table of results. Or maybe uh since joins are involved, now you're gonna like blow the thing out with a bunch of beta. I need to go execute this query over here, which gives me one table of results. I need to go execute this query over here, which gives me a second table of results. I need to send those tables to the client where it can then take the small or two tables and join them together. At the moment of time it needs to do the lookups. But you've given it all the data it needs to do that reliably. And so I guess all this is starting to point in the direction of if you're to build one of these systems for real and not just rely on a bunch of client side manipulation. You would need to basically materialize something of a plan something of a here's the dependency graph here's how we're going to get from source tables into the actual tables we need optimize that. But then you've always got this added wrinkle which is the user is building something that is interactive with two or three clicks they can instantly change the columns that they need available they can do all sorts of craziness that might require some giant materialization. And so if the source data is a couple of gigabytes you're always a mistake or two away from requesting a couple of gigabytes. If you might lose some work. Um proper client server architecture, what's that mean? Well, it means you've tasked the server with going and materializing a couple million rows. It needs to actually go allocate a couple dozen probably gigabytes of RAM. It then needs to somehow like serialize that into JSON. It's probably gonna fail there because through that step, it's gonna transfer on the wire. Somewhere in there, the thing's gonna fail. And so instead of doing all that madness, I just encoded a hard limit. Hey, you only get so many rows, you can change the number of rows. But if you know you're bumping up against this row limit, you should probably do something to filter where you're looking at fewer rows from the start. Um that's one way to do it. That's a very cruise filter. Uh cap the total number of rows. Again, a different way of doing it is to more smartly go identify the uh exact rows and columns that are needed, only send what's needed to support the charts that are being rendered. You could even go so far as to do uh the aggregations on the server and only send across the aggregated results. If you know you're just building a bunch of row charts that are just aggregated results, we don't need to send a million rows just to go take their average. You can take the average and send it. But then all of a sudden, if the user says, Oh, actually, you can scatter chart of those. Now you're right back to where you started, which is it's always they've always got the ability to request a chart that requires all data on the end. Now granted they can't easily go build a chart that request every single column, but you still have some ability to choose which columns go across. But then as soon as I say that, one of the like uh commonly used features here if you're doing all this client-size visualization, is you just click a button and you instantly get a chart of every column showing its natural distribution. Um yeah, pull up thread. I mean it's showing its natural distribution, so again, we've got an aggregation in there. So I could, if I wanted, pre-compute the aggregation and turn that over. All right, I'm buying that. Um so I can at least avoid the uh diabolical case of hey on first load I need 30 gigs of data. Like now on first load you need uh 30 charts, which are aggregations. Now there may be a diabolical case where you actually have to go compute the aggregations, uh, but then also well, depending on the backing store, there's probably a uh less intensive way to either compute the distribution on demand or to be tracking the distribution as you go so that it's just available. Um, and if you had complete control over the data store side of things, this is probably something you would do, just build in these distributions. Now the distributions are gonna fall apart really quickly once you start filtering. And so again, you've probably recovered a lot of the problems you were trying to avoid. All right, uh so that's a lot of words still about this idea of uh source data, configuration, filter layer, and this tension of having all the data on hand versus getting it on demand and how that plays into the experience. Uh it's probably worth talking just a little bit about what are the like auxiliary features of the application that really benefit from having all data on hand. And yeah, it's like cheap and easy if you have all data to go do stuff with it. But like what do you actually like? What do you actually need that for? You're gonna go display it to the user, okay. Wow. Uh first consideration. If you have all data, you can go determine all the keys of the data. So here's every possible thing that I could go plot. And in the case of Shifty, it had this odd behavior, it was desirable but on, where a analysis result was stored as JSON. And so for every single analysis result, it could technically contain different keys. Depending on exactly how to write the calculation, you might have certain keys be available. You could even technically have it so that sort of the data type was changing. Basically, it was a completely schema-less approach to storing results. And so one of the things I did was basically send over all the keys. That way, any spot where there was a field picker, say, what do you want to plot here? I can give a really nice list of options. It had a fuzzy filter, you could type, and it just made it really clean and simple for the user to go pick the fields they wanted to block. Because I had all the data, which means I could go find all the keys. Now, once we got to bigger data sets where you're doing the uh column subsetting stuff, you've got a new problem, which is if I want to present the user a field picker, but I'm only relying on client side fields. Well, my picker's not going to be able to expose new stuff. And so then I had some functionality that the server could basically go run through the database, pull out the keys, and then those could be sent alongside the columns that were actually extracted. So you've had a complete field list, but uh nice data set that was truncated or filtered, subsetted. That worked. Okay, so field listing is one of these things where it's really nice to have all the data. What else benefits from having all the data? Um automatic limit detection, which is to say very often when you're doing these dashboard analysis tasks, you want to have a set of uh let's say scale limits, whether it's X, Y, color, you want to have a set of limits that are aware of the total range of the data. So, for example, maybe I know in advance that the engine speed is roughly zero to three thousand RPM. Well, that's fine and good and all until you get a gap lean engine in there and it goes up to 5,000 RPM. And so if you've hard coded in advance, hey, this parameter goes from zero to three thousand, and then all of a sudden you're charting data that goes up to 5,000. Well, if your chart is like truly clicked at 3,000, you don't even know that data is up there. He's not gonna see it. Um you might choose uh let's render it at the max, and so you have this like ceiling sort of thing, that's terrible. You come up with some sort of uh UI affordance to indicate that there's data out of range, might work. Um creating chart limits, they tend to go much better if you basically have a snapshot of the max and then of the data that we can build the chart to adapt to that. And that tends to work the best. So if you can send all data to the client, by definition, these charts will render correctly the first time they load. Even if the clients maybe when they're looking at a subset of the data, uh this gives executive difference between like working data and visible data. If they're working data can see the full limits, you can go make the charts adapt to the full limits. That way, even if the visible data is some smaller range, you've got the ability to have the chart scale to the limits on the working data. So that's an area where having all the data in advance is helpful. And really that applies to uh kind of all right, what's like the general principle there? Well, you've got a numerical data type, something that has uh you know, it's continuous, numeric, so you can define kind of the limits of that with a minute max, which then lets you set up chart limits, x and a y scale. It also lets you pre-compute a better color scale, because now you can adjust the color scale so that you're not constantly saturating on one end or the other. Um, so that's uh that's the numerical scales. Uh so there it's kind of like the limiting principle is for a numeric data type, it's really helpful to know the min, the max at least. It can sometimes be helpful to have the distribution, because maybe you want to do something where you're looking at the most common values as opposed to the extreme range. Maybe you can detect outliers and then go set the scale, or somehow flag some sort of data quality problem. Alright, what other data types are there? Uh categorical or string. What's happened there? Well, if you have all the data in advance, you can very quickly go determine what are the possible values. And now you're into the world of what is the cardinality of that. Am I looking at something like a GUID where it's you know the cardinality matches the row count? Every single one is unique. Am I looking at something like a user-entered category where I've got two, three, four values, and that's it. Or maybe I'm looking at something that's uh sort of uh like auto-generated. Most common example was like file path or folder path. Folder path is better because it would tend to be the case that your data files were in folders. Those folders tended to map to maybe a test type or a date. You might do the folders year, month, day style. And so data files in the same folder. It was effectively encoding the date. But the way this is coming through is in a string. And so this gives you a string, which is categorical. And maybe you would have on a thousand files, uh 30 dates, 60 dates, 90 dates, more than three, but way less than a thousand. And so that's kind of this mid-cardinality thing where you would struggle to build a color scale on that, like assigning a unique color to every single one. But you could build a meaningful kind of uh drop-down filter bicker sort of thing. Let me go filter ones that have this in their name. Uh what else is helpful? You have all data. Um, well, you can instantly see when nulls are present, and so nulls wreak havoc on everything. Um how to deal with nulls is one of these just constant struggles, probably worth its own talk. But uh knowing that there are nulls is important, knowing that there could be nulls is important. Knowing what proportion of the data is nulls can be very important. Uming that all data is null is also important, so everything is null. Um just for a quick discussion on that. I chose to treat null differently if it was in a numerical column versus a categorical column. If it was numerical, you're basically just stuck. There's no good way to present null for a numerical distribution. But the best you can do is to add an extra column to the end, which is null. And now you run the risk of everything really throwing off the distribution if the data's half null or something. Um, so you can't do that. We actually do that with the really usual AI stuff. Uh huh, we present a null column off to the side. It's got some max height, so really it's an indicator of null presence. In theory, you could hover over it and get the actual proportion, so that might be good. Uh, for categorical data, I would just render null or undefined as a category that you could see. And this was uh mostly pretty good. It gave you the ability to see that they were there. Most chart types most of the time. You would not end up with these overwhelming undefined bars. And so it mostly just worked. But then presenting that as a like a category or string value came with all of its own sort of difficulty in the analysis pipeline. Because now you've got the saying that looks like a value but is encoding undefined. All of this data was being built from calculation. So you could technically divide by zero. We could attempt to add two null numbers, add a null to a non-null. And so then there's a pipeline of how to handle moles at different spots. In some cases, they were treated like zero, in other cases they were truly ignored. And so getting consistency on that across the whole application was a bit of a mess, which is say computing the average of 10 values, five of which are null. You probably want to just throw alpha nulls and take the sum and divide by five. If you're going to do that on the back end, and you present another average calculation on the front end for some reason, you need to make sure you've got consistent logic there. It's a little bit of a mess. Undefined also has a bad property of crashing stuff on the front end frequently. And so you'd often discover undefined and unexpected places after it crashed some bit of the logic. Okay, what else can be helpful if you have all the data? I mean, I think that's honestly it. It's like you can with confidence say things about the distribution of the data, about its nature and kind, about the quality of the data. If you go do data audits in the charts themselves, because you can see everything. Once you start moving to this model of, I'm going to do all of the aggregation and analysis on the back end and just send the results to the front end. It's much harder for the user to kind of stumble into doing their own sort of quality work because in a lot of cases the things they're trying to find are the things that you're going to filter out accidentally or on purpose in some of these back end analysis tasks. Okay, but this does point toward something that's a very critical feature. And so let's end with this, which is to extend I've got all these source tables and I know something about them. It is really, really, really helpful to be able to see all of the source tables and their fields and their relationships and their data types in a single view. And so honestly, the database style ER diagram can be very helpful. Just listing the fields with their data type, showing the relationships can be very helpful. Be able to click on a table and see a subset of rows, including all columns. They give me 200 rows every column, can be very helpful. Doing these data explorer things can be very, very helpful. So actually show the distribution. Go run some of these data quality things. Maybe split it where you're rendering stuff on the front end, but you're doing analysis work on the back end by remote sending millions of rows. And I talked a bit in my interactive data tables notes about all of the features that make those nice. Being able to interactively explore source tables, and then immediately bring those down into a dashboard. Drag a field, right click a field, select two fields, right click, make scatter, that kind of thing. Now you're starting to like weave together this really powerful exploration with the visualizations. See what they are, let me go make charts based off of that. This honestly is one of the areas where shifting was really weak. In hindsight would have actually been really, really, really easy to go make. I had all the data available. Um so something that kind of gets you in this mentality. Let me just introspect what's in my data would have been really helpful. What shift you presented at the beginning was just a single giant text box, a text box, but like a combo box select thing. And so if you wanted to know if a field was available, you'd just type part of its name in there, hit filter, you'd see it. But uh it's just a terrible experience in hindsight. Um I could have very very quickly shown the different tables, put some counts on it. Yeah, and I did start doing that. I had this like giant table at the top that would present the uh counts at least because one of the things that would happen frequently is people would manage to find their events but not run the analysis, and so you'd expect the event count to match the analysis count, unless there were errors or omissions, and so just surfacing that was done there of a problem. Um even there it was uh this giant grid thing that looked terrible. Um, it could have used a bit of polish to really say what is the goal of this thing. Okay, what's the best form if that is the goal? Making users compare uh counts across 13 columns is not particularly helpful if you know that's what they're doing. Just report hey, this one is missing. This one is missing something. Uh we can see that the counts are not the same. Uh and then and then at that point you might surface the details about oh, this this one here actually had five errors. Do you want to just go look at this? That would have been a way better experience. Cool. Okay, so source table exploration, and then related to source table exploration, you've got the raw hot source tables. But all of the uh calculations and transformations I'm describing, those realistically live kind of in that source table view. It's almost like you're showing oh, here's the fields that I've added, here's how they depend on other things. Okay. I think the other wrinkle is this idea of like just giving a hint at differences in cardinality, because very often once you know that things have different counts, it starts to make abundantly clear what the possible relationships are, or if you've got problems, because I expected that to be one to one. But we're 50 rows short on this table here. Uh you can imagine running, oh, what's going on there? Do a data quality investigation. And then you could go and make sense of which keys are mismatched. It's like, what is the goal of all this interactive data exploration? Well, sometimes you're trying to truly arrive at findings, like my data is good enough. Here is the results of the analysis. Engine speed causes main flow to vary in this way. Um but in other cases it's pretty confident we ran a good task. But did we? It's just sort of this quality existence kind of question. Did I get all the data I expected? What's missing? Why is it missing? Did I plan to run that test point? I like failed to. Temperature was misconfigured. Or I just completely like did not plan on running that point. But now that I'm looking at my chart, it's clear that I wished I would have. Those are like different sides of the data quality coin. One is good plan for execution. The other is the sort of like serendipitous discovery. Oh that's the region that matters. And then you actually have to go rerun the test. Or you're having to kind of guess the results through that area. This is well, I didn't actually run that data point but here's what I think I would have gotten if I did. But again I'm going to interpolate the two points I ran on either side. Okay. Yes that's a I guess I'm going to say like a far-reaching discussion. Again on this idea of uh source tables filtering uh introspection over them all of that sort of stuff we'll have more to come on the actual uh chart types and the interactivity there especially around faceting that was something I thought I'd talk about