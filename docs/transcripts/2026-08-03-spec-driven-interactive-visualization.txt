Alright, here we go. It's going to be a note that continues the idea on interactive data visualization. Specifically, this note's going to continue talking about the idea of the data viz being driven by a spec and basically how to define the full sequence of everything in order to arrive at a data biz. And on the one hand you could start just with the chart itself. So you could say I've got a bar chart. And you could define the bar chart by having some x-axis, which is maybe a count, and then some y-axis, which gives you the rows. I guess I'll say bar chart, but really I mean a row chart. I've got a row chart. And the row chart has this idea of well either I'm gonna go plot the raw values, say if it was our revenue over so many quarters. Quarter one, quarter two, quarter three, quarter four, I've got four bars, uh 80, 70, 90, 60. So we set the X scale to 100 and go written them accordingly. So that's one version of a row chart. The other version of a row chart is where we're using it as an interactive filter. And so instead of just plotting the value directly, we've got a transform in there. Maybe I've got a thousand rows which contain quarter one, two, three, four. Nice. Want to go count the rows. Maybe it represents sales. Uh each line has a sail. Independent of revenue over anything else. And so then I'm gonna go add a transform which aggregates over quarter, does the counting. And so now I'm rendering same idea, a table of quarter labels with some number for the quarters. In this case the number corresponds to a count. In the previous case it corresponded to some revenue. But in both cases it's a quarter label with a value. We can go apply the same logic. Compute the X scale. Determine the available space. Once you know the available space, that sort of dictates how the Y scale is going to behave. We're trying to evenly separate four bars. Uh in both cases is going to arrive at a row chart that looks quite similar to each other. Quarter label, some number. And so this is all good and simple. But maybe what's really going on is this chart was derived from some specific customer. We're actually showing we're in a dashboard where we've got some bigger chart or some bigger data set, which is all orders from all customers. We happen to be filtered by a single customer. Now when we're plotting orders, really it's in the context of this single customer. And so the bars that I'm getting make sense given the data I was given. What I'd really like to capture is this whole process, all data being filtered, being rendered. And then this is not just a single chart, but this is three, four, five related charts in a dashboard. And so that whole arc there of I have source tables being combined in different ways to give multiple interrelated charts on a dashboard. efficiency at least efficiency in the user is you need these interactive dashboard things to bake in a ton of default functionality or default decisions that way the user is not having to specify 30 things just to get some pixels to change and so instead you let them specify the simplest stuff like I want a row chart of quarter labels and since we're kind of already in this dashboarding context it'll be assumed that we're doing the aggregate count also since we're in a dashboarding context and this chart is in a group or panel or region we already know that we're going to apply the filters that are active in that region so we already know that somebody has chosen a specific customer because this little panel is the customer specific sales portion and so with all that info we can see kind of what the spec is actually going to describe and this honestly gets really really close to how specs were stored in shifty which is that I had some default set of chart types that were supported those chart types give the user options go pick your fields go override your scales go override any of the labels if you wanted to. You could potentially set filters on a chart, or if you did not set filters on a chart, your chart data was influenced by the global filters. And so every single chart had a spec. It's basically just the flat JSON sort of thing, key value stuff. Then a dashboard was built by having a list of specs. Some of those specs included the filter details. And so once you put it all together, you could go and reconstitute the client by reading out the spec list. The client would take care of seeing which things were filtered, it would apply all the filters again. And all the charts could basically reconstitute or rehydrate from the spec. The first of which is that only supported overrides existed. Related. There was no sense of like islands of filtering. And so I was not able to indicate hey, these charts are coming from this data source, whereas these charts over here are coming from this data source. And so that's the sort of thing you would like for a spec to be able to handle, which is to declare here are my data sources, here are my intermediate transformations, here are all of the variables that I'm going to make available, and here's how they are defined. And so all that was kind of implied that the app layer. A full chain all the way from source through transformations through selections and any intermediate logic or math all the way through to things like uh go compute the scale limit doing this sort of thing. Have that stuff be aware of or respect something like a faceting constraint. Hey, I want to repeat this chart this number of times. I want to repeat this visual element this number of times, or more likely I want to repeat this visual element for this series of choices. And then I guess it's worth considering this idea of repeating. That's almost like the core of every chart. Repeat this bar over this data series. Repeat this scattered point over this data series. It's almost like you're just sort of one level higher. Repeat this chart over this data where the subdata comes from a filtering operation. So it's probably an elegant way to make that work out where you're just everything is repeats or singles, and even a single is just a repeat, uh one item less, just to avoid the one verse many consideration. You're drawing line segments between data points. What are you really doing? You are computing xy coordinates after applying some sorting operation. And so even that is its own sort of transform. I don't know if it's worth being that pedantic about it. It's almost like if you're gonna have the concept of a line chart, you almost want to just imply that it's x versus y and that the x values are ordered, or that it's x versus y with an ordering. That's so so common that it certainly seems like a real waste to say, oh here you can mainly specify this diffing operation. But then if you are being pedantic about it, maybe that is the internal transformation that's helpful. That way you can actually go compute, hey, where did this x value come from? That x value came from taking uh the x's and y's that came in, really taking the x's that came in uh and ordering them and then plotting them in a given order. I guess you're not really diffing, you're just sort of going from xy coordinate to xy coordinate. Um because if you have the node x and y, let's say in the engineering units, you're gonna run it through a scale. And that's what's going to give you the actual pixel values. And so that's the same thing you would have been doing anyways. Okay. Um I guess it is the case that you're saying where did this x and y coordinate for this point come from? The point itself is just the xy, that's that. So if you want to say where did this line segment come from, then the line segment really is derived from this connect previous to current kind of logic. Um you draw a line segment for the first data point. You need a previous. Then again, it's so common. Like would you ever do anything else? Um yeah, I guess you might, because let's imagine you're making a slope graph instead. What defines a slope graph? Well you've got basically uh probably an x value and then two y values. X, y1, y2. Or maybe if you got y1 and y2, point three. You're gonna go connect those three. And so then if you're having to say, well, what's the logic to go draw those lines? Well, they're drawn by just taking these exact values um but even still if like at the end of the day there is a like a vector in there but maybe there is something to be said for uh I'm not I'm supplying the values just from here from here it's not like I'm supplying a whole column of values necessarily I'm building an array of values from one two three choices but then that is its own transformation what's going on there while I'm storing a vector of values how's it built I go and take these three values and I pack them together into a vector I feed that vector into a line drawing my algorithm and uh yeah what is the y coordinate the y coordinate's coming from the values I guess what's the x coordinate I guess that's where it gets tricky because if you're doing a slope graph thing it's like what is the x coordinate well you're basically gonna go and create columns based off of some some natural split on the data A B C. I guess maybe what's going on there is it's like you're building that thing out. But the it's like that you're gonna end up with a line per row. That's what's different about it. Because normally what's going on is you're just plotting a line. In this case, it's like I want a line per row. And my data's already wide. So it's not like I'm doing a grouping and then creating the line. Like if I just don't all the math to compute the three points that I want to be connected. And so like somewhere in there, I'm gonna play with the idea of in my table. Well it's like you've got a table of rows. Okay, I've got uh uh. And I've got three conditions, A, B, C. And I basically want to go plot a line for each of the ten. And so it's not that I'm wanting to go plot the line. It's not that I need three lines, one for A, one for B, one for C. It's that I need ten lines that each have three points. So this is different. And so what would be the natural way to call that out? Well, I'm gonna go draw a line for each row. I want to uh have the y values to be derived from uh I think this is where you would just go embed an object into the table. Do I have three columns? A, B, C, and I got the fourth column call slope Y. And inside there, I'm joining together A, B, and C to make a vector. And so then when I go to make my lines, what am I plotting? Well, each line is drawn from uh slope y. And obviously I've got a line for each row. Yeah, it's it's that idea. I've got a line for each row, or I've got a line for each column. Normally we're doing a line per column. But here we're doing a line per row. Sometimes we're doing a line per column where we've grouped the columns together first. Excuse me, we're group the rows together. That's a really we're gonna end up with 10 columns, let's say. And so then if I want to think about my slip graph thing, well, is it a line per column? Yeah, sure, why not? But now I need to go end up with 10 columns, which is like my rows are now technically A, B, C. I've got 10 columns. That's a very odd transposition to require. Okay, before you can go and make the slope graph, you first have to transpose all your rows into columns. I'm trying to keep track of them. I seems silly. Especially labeling the columns. And so this idea of I want to go draw a line where the data is coming from each row. That feels very natural. And whether you explicitly create the like incatenate into array operation and embed it as a source table. Or you make that somehow a first class thing just in a UI. You can pick these values. There's something kind of fundamental going on there, which is that I've got the ability to create a glyph over some repeating condition. And then the whole chart is kind of its own. Well, I've got the ability to create all of these glyphs over the source data. And then express, hey, this chart gets drawn differently with this other source data. But it's the exact same drawing algorithm. It's the exact same drawing rules, it's the exact same glyphs with the exact same logic. Just being either then a different slice of data. Possibly different slices of data if it's coming from multiple tables. Alright, so if we had to go look at the spec that's mapped in that. We have the idea of these kind of built-in chart types. I've got a bar chart, got a row chart, got a scatter chart. I got a scatter chart where I connect things into groups and then draw a line through them. What is that really? What is that really? I've got a scatter chart where I'm gonna connect things. Let's put things into groups. So I'm taking uh hundred rows, three groups, 3030, 40. Drawing a line through the data. How am I drawing the line? Well one option is just to go connect the dots. You can do it. I go connect the dots. So I've got some ordering and I connect the dots. And so here we're just drawing a line again through a column of numbers. But let's say I want to compute a regression first. So I do my regression. I compute a regression with each subset of data. Okay, so let's say I'm doing that. So then this idea of the color variable. It's now doing something different. It's not just oh group these and color them. It's actually like grouping. Alright, so what does that mean? Grouping. For grouping. What's grouping? Um simple version of grouping is I'm creating three new subsets of data. I want to split my hundred rows of data into three groups of data. And for each group, I want to go draw points where the color is driven by the group. That's one version. I want to take my groups. I want to compute their center. But I'm just coloring the scattered points. It doesn't really matter. It doesn't really matter that they're grouped. Because you get the same result. If you know the categorical variable, you get the same result whether you just render them all, have color be driven, or you split them into groups, and then have all of their colors be the same. But the second you want to go draw a regression line through them. Go connect the points. And something different is quite important. Because I don't want to have to write a bunch of logic in the scatter chart that describes how to draw points with groups and then connect them on a line. What I'd really like to do is identify that we're doing the grouping and then have the drawing be driven by the groups. And so maybe it's a maybe it's the case that if we're gonna go color by some variable, maybe we do just go ahead and split them into groups. To me that does not seem intuitive. It seems way more intuitive that it's like I'm a grouping and now I'm gonna go draw the line through the groups. And then go turn that into glyphs. Because in this case we want to go draw a regression line for each group. And then when I go to draw the lines, what are the x and y coordinates? Well, we're just gonna pick some sampling value. I want 100 points and then evenly separate them on the x-axis. I want to draw a line to 100 points. I want to sample on points that we measured. And so now the points that I'm sampling are determined from somewhere else. I want to uh show a continuous line, no points. Well, you saw points, you just or maybe rendering the line slightly differently, or you're doing segments so close that you can't see them. Or you're computing some path with uh smooth sections. Okay. So what does this spec look like for all that? Because what's going on is the groups that you're gonna end up with you don't necessarily know until you have the data. Now you might know the groups in advance, and then you could handle that accordingly. And so I think that's really fundamental to sort of uh spec things is you either need to say for each unique value in some list. Basically we're gonna go determine the key for the index, or you say for each in, it's like I'm gonna give you the list. You don't need to go determine it based off uniqueness. Here it is. Only render these. And so then what are we doing? We're splitting. One way to think about the split is that you're creating a new column that's sparse. So that might work. Another way to think about the split is that you're actually creating smaller subtables that are possibly different length. I'm not sure if there's a pro or a con on either of those the uh sub tables of a given length honestly feels simpler if you're trying to show it to the user and you don't have to differentiate between the nulls that you've created and the nulls that exist in the data. Alright that's one option okay so I'll say I like that one split it into little small tables. Probably not the most memory efficient thing to do. I'm actually going to have an operation just to split into groups. I'm going to go compute something. Group. Alright, we'll have to think about that one more.