Alright, here we go. This note is going to continue the discussion of specs in charts. Specifically the idea of more advanced charts and some of the common patterns, common needs, and just thinking about how to work them into some sort of a spec type system. And there's been a lot of discussion in the other notes around specifically scatter plots, their coloring, their grouping, and what it takes to go draw regression lines through groups of points. That's an example that I always come back to because it kind of highlights pretty much all of the complexity you're going to face in these systems. And if you can come up with a way to render that scenario, you can probably adapt it to handle most other stuff. And so that scenario is specifically a scatter chart, Y versus X, split on a color variable, and then faceted over rows and columns where you want regression lines to be computed uniquely for each facet cell, and where you're showing some sort of line or annotation that's computed off of some global bits of data or something else. So basically you're rendering a bunch of charts that have a common template wherein the calculations and logic needs to be executed uniquely for every subset of data in your facets. But then separately you have some other bits of information that need to be computed at the global level and then fly into each chart with no consideration modification for the facet. And probably the simplest way to think about this is imagine you're computing some global limit some per range limit which might be a facet column and then some per I don't know what commanded pressure level limit which might be the facet row. So basically you're computing something for each group grouping each level of the hierarchy and you need all those values to be computed to fly in and once they have flown in you need to have complete traceability from curricula back to its source data. So when I go and click on a regression line I see the n subset of data which is that facet row object column filtered by that color variable. And I've computed a regression line on that narrow subset of points. And so all of that seems pretty doable. One upside about scatterplots is that there's no math required. Position of glyphs. Oh. Alright, so this note is going to talk about the slightly more complicated scenarios where we are required to consider the position of other points when we're rendering a glyph. And so there's a handful of chart types that come to mind. First is let's just do the stacked bar because I think that's the easiest one to think about. So in the stacked bar, we've got typically some sort of X variable, which is either categorical or numerical. Let's pretend for a second that it is numerical, why not? We're computing some sort of distribution or histogram. So maybe it's the distribution of engine throttle. So it's a continuous variable that ranges from zero to 100. If you're looking closely, you know, it might be normally distributed around 70% with some odd peaks at 0% again 100%. And so then what we might want to see. Where we've got our default distribution. And so this is just your normal density versus x value. Maybe we're doing counts that keep it simple. We got 10,000 rows of data. So our 10,000 values are getting distributed across bins. We have to go determine the bend size. Maybe we do some fancy algorithm to get the bin size, or maybe we cheat and just say, ah, make 5%. So 5%, we got 20 or 21 bins, can't be count. There's 19. I don't know. One of those three. Excuse on the high side. With maybe the last bend being the exception. Something like that. Yeah, that all sounds right. Because if the exact value is 60, you expect 60 to appear in the bin called 60. But if the final bin goes up to 100, we expect 100 to appear on there. So the final bin can be exclusive at the max. And now we have a rule, which is that maybe we'll create a Y scale where the min and max on the Y scale go from zero to max count. And then we might uh choose to pad that thing out so that it can go up to max count plus 10% or something. We can clearly read our labels. And now we're gonna go. Go and render that thing. We've got a nice distribution. We can clearly see what's going on. We click on a bar. We've got some understanding that bars are mapped by value, and so sort of like a filtering operation that somehow we can work our way backwards from the bar to the individual items. This is good. And so if we originally just showing Drummond, we might like to know what is the commanded range or state on the system. And let's just do uh shift state. Are we shifting or are we steady state? And so that gives us uh two categories. Shifting, steady state, you can think of the boolean. And then maybe we throw in a uh third state, just because three will slightly complicate the math, that'll be good. Uh we've got shifting, not shifting, and how about uh shift is commanded. So we need a shift, we are shifting for steady state close enough. So now what we want to do is we want to slice the bar we just rendered. So imagine we had uh 500 items in there, so we got a bar that's going from zero to 500. It's uh Y scale is dictating that 500 goes to some pixel value. The bar's got a height of 80 pixels maybe enough. But then we go and we want to stack those bars, and so what is a typical stacked bar look like? Well you take the original bar, we subdivide it. And so if we have three items, then our original bar, which maybe had three three categories, we had 500 items. Maybe it's just 200, 200, 100. And so what we want to do is render three rectangles where we previously had one rectangle. And the rule is this the heights of the new rectangles must fill the space that was previously allotted for the big rectangle. That is, this is a transformation that is bumpless. We are simply subdividing or choosing to render the rectangle into subgroups. And so now we arrived at our problem, which is that for the first bar we need to go compute what's the height of category A. It's pretty straightforward. We had 200 items in there. We run it through our scale. We end up with 30 pixel, something like that. And so we can just render a box from 0 to 30. Life is good. But the second box, it has a problem. It needs to render, not starting from zero, but instead starting from the top of the previous bar. That is, we are accumulating and so now the starting position is got this baseline applied and then what's the height the height is actually pretty straightforward because if we knew what the pixel value of the height was with the original scale actually ends up being the same thing so in this case height if you're just doing height of the bar you can cheat and just say oh well I know that uh it's 200 units high again and so it's 80 pixel now that's gonna fail you when the scale becomes nonlinear and so you really can't do it like that instead what you have to do is you have to separately determine the position of the baseline and then separately determine the position of the endpoint and once you have those two you run them through your scale and then you use the y2 and y1 as the positions for the rectangle you take their difference to get the height or you can just render it from those y coordinates and so what does that mean? Well it means that if we're trying to render this box floating in air that's another way to think about it as well if you hide the first series the new one's just floating it's floating in air and the position for the lower side is the previous accumulated position. So for the first one, it's just the one before it. Well, for the first one, it's zero, for the second one, it's the one before it. And then for the end position, you take the start and then you add on the height of the rectangle in engineering units, run it through the scale, that gets you the pixel value. And for the third one, you take the uh final engineering units as your start. And then you add on its value. That gives you two more values. So it basically puts you in a position where you're starting it where the previous one left off. Now this gives you a slight issue because if you're gonna draw these things with any sort of uh stroke, you basically have two lines that are directly on top of each other. And so you can be subtly off by the stroke of the rectangle if you're going to include a stroke on it. Now if you don't stroke the rectangle, now you just have two rectangles that are drawing to the same order. If you're going to include stroke, you basically end up with overlapping strokes. Or maybe you have to define the stroke so that it renders on the inside of the shape. What you don't want to do is accidentally render it with the stroke on the outside of the shape, and now the thing is uh misrepresenting its size, at least in the image. Obviously, you can render whatever you need to be correct for the scale text or grid. Okay, so now we got this uh got this idea that in order to do a stack bar, there's a transform in there, and the transform needs to basically be accumulating positions because the data series coming in give you some useful information, but then we need to order the rows by some categorical variable, count them, sum them, do whatever to them, and then get this accumulating baseline. So that when we go to render the glyphs, they show up in the correct spot. And if you do this correctly, you should be able to get a stacked bar chart where the user is just providing uh x coordinate and the grouping coordinate. Again, that assumes that you're wanting to do a distribution. Uh the other possibility is that you don't actually need to do the distribution. Maybe you've already done your aggregation. Maybe you're staring at a pivot table. The pivot table is giving you uh revenue over time for each product category. And so time is your X variable, you've got months. Uh May, June, July, August, September. You've got uh groups, home furnishings, uh checkout, candy, and I don't know what else. Fool spots. You've got these three things. And in that case, you're just staring at the values that be plotted. I'm looking at five months, three categories. I've got 15 cells. I just need to go render rectangles that apply this baseline logic. So I think that kind of puts you in a spot where, again, the value being plotted, it possibly has a transform being applied because you need to aggregate the data before you go to visualize it. This is to say I have raw data and I need the pivot table. And I don't know what we want to call it. I guess it's like a visual transform. It exists exclusively to compute the positions for the glyphs so that they look correct. And they're nearly always following a handful of these accumulating doing something a little bit different. So now you're possibly looking at the physic table. You may not actually show it, but you could uh but that pivot table did not exist in the raw beta. It only existed because you did the math on the top. And separately you're doing math to go render these clips. And so what's that math need scale? Yeah you get position things on the X Y scale yeah. But then the Y scale is not the final story on how you choose the actual Y position because we need this accumulating idea between points and the accumulation is with respect to an ordering because I might choose as typical you put the fattest category at the bottom but then fattest category at the bottom only makes sense possibly for one of the bars because it's entirely possible that a big category in one bar is not the big category in the next bar. You still have the you still have this problem you want like an ordering on the bars that's consistent horizontally like across the ball so somehow you need to determine what's the correct order on the bars. And then once you know that, you can go do the accumulating math. And the accumulating math is not hard. It's honestly real similar to the logic of go draw a line between these points. Just like we're taking uh ordering, a delta, uh, and then doing the math. In this case, we're not taking the delta, we're just like accumulating the positions. And you can do the accumulation on the engineering units or on the yeah, it has to be on the engineering units. You have to do the accumulation before you do the scaling, because it's entirely possible that you'll choose to do something goofy with the scale. Is that correct? Trying to think through if it's logarithmic, which one's correct. Um but then if it's uh like a discontinuous axis, like if you get some gnarly logic in there that's choosing not to render certain y values. I don't have to think about that one way or the other. Alright, so this is one example, the stacked bar. And the nature of the stacked bar is that the uh one of the positions, in this case the y position, is determined uh using all of the preceding y values. And if you did a stacked row chart, you'd have the exact same logic of all of the horizontal positions, the district, we run through in order that way they can accumulate and stack. In case they're stacking from left to right. And a key feature there is the ordering. And so you need to bring that group variable along for the ride. Or you need to indicate, okay, the color of the cell is determined by a color scale and a lookup. Okay, so that's one version where we're positioning. With another version that gets complicated. Let's do let's do let's do a stacked area chart because this is similar to stacked bars. The difference is that you're connecting segments, I think. Like I think honestly it's all the exact same math. It's just now you're drawing polygons between the points. So in the case of the stacked bar, you just have to uh determine the uh upper y coordinate, I mean upper and lower. That's the upper is the one that matters the most. You gotta determine the upper y coordinate. I think for a like a stacked area thing, you're doing the same math. It's just now your uh connecting points across the x-axis, and so it's basically go compute all of the y2 positions, all the upper y positions, and then you're saying go draw a line through those. So I know my positions, I want to go draw a line through those. And now you've got the logic for positioning. Oh, I need to go determine these y twos. I'm doing the odd thing of sort of let me take those and now connect them. So I've got what is basically a row of data, and I'm connecting it. And then to draw the line, I have a separate problem of I need to go from the previous point to the next point in order to draw my line. So it's like I got this layering of visual transformations to make it all work out. And so for the line, I think you reach a point where the line technically you can express the line either in engineering units or pixels. I'm not sure that's gonna matter until if you're on a scale where you need to somehow interpolate between points. I'm assuming now you'll just draw a line. But if you need to uh if you've got a log scale or something, you're actually trying to like interpolate in time. Well now you may need to do something a little trickier. So I think then you want to do all of your uh what points does the line go through math. I think you need to do that in the engineering units, and then you apply the scale to go render it, and that will give you results that are correct. And then if you need to avoid artifacts, you go and sample at a finer interval so you can match the curvature of whatever your scale is called me. Really, it's gonna be a you know a log scale on like a time axis or something, a log scale on a y axis, and then you plot in a time variable, and now you're having to fill in data points, but it's a log scale. Next up in terms of complexity, it's going to be something like a uh you know like a theme graph. And this is where you're same game showing accumulating values. These often have the hallmark of certain series are coming and going. And so then you're having to choose how to render uh given series, what their color is. But I guess at the end of the day, I mean the theme graph is basically the same thing as these other two. It's just you're rendering, I mean just a stack of bar chart, you're just choosing to connect the bars with a smooth shape rather than a segmented rectangle. And then ah, maybe this is relevant. The idea that nodes are like in order, you might choose to relax that. Imagine you're doing most popular genre over time, and so you've got some ordering of uh genres, album sales, time, and what you're wanting to show is the expanding and collapsing as things get more popular. And so if you're gonna do that sort of thing, you kind of want things to look good from left to right, which means you need the bars to be contiguous. Which means you're gonna have to do some logic to figure out what is the ideal position. Yeah, you are doing accumulating. Can't get away from that. But in addition to accumulating, you're doing some sort of a path from left to right when you want to ensure that related cells are connected to each other. Or that you tried to keep them connected. Now some of the time it's just a terrible visualization. That's simple enough. But you might very well have oh the genre was popular in the 1900s. This genre was popular in the 2000s. There's a giant gap in the middle. You just can't draw a contiguous thing there. And so then you might have to do a thin segment connecting them. Maybe it looks terrible. You might have to use Hover to do emphasis and then show the two regions. But you would do your best to make things touch if you could. Alright, so here we've got the same sort of idea of I've got some engineering units because the like heights represented in a given grouping are like known things. But then I'm converting those heights into positions using my scale. I'm connecting positions from left to right using lines. Those lines are likely going to be smooth instead of just straight. So we're gonna have some sort of uh line or something going across them. And then there's some additional logic running to determine the vertical positioning of the segments. So you get a clean visual from left to right. And you could very well imagine letting the user kind of move a blob of stuff up or down in kind of like final space. And really what you're changing there is like the desired ordering. You're changing some sort of almost like parameter or constraint inside that middle algorithm that's choosing how to position things left to right, up and down. It paints a picture that you want to paint. Maybe helpfully, not maliciously or deceptively, but like, hey, this graph would make a whole lot more sense if I could get these genres together. Yeah, maybe that's consideration. I'm actually trying to show two layers of categorical information. And if I can get them all together, that ordering is better than the default. But then you might still want to come in and be able to move these things around. So you've almost got this. I know I want to order them like this, it still needs to get solved. That way the heights come up correctly. So you'd run the solver, but you're giving it some nudging. Okay, that's going to feel pretty good. And I think it's clear in there that there's some step wherein you have your table of values. I'm effectively looking at a set of rows and columns where every group is a row. Every column represents a time, something on the x-axis. At the intersection of the row column, each cell is the value for that group in that column. Very simple. And you're free to order the bars however you want, because the whole game is uh come up with a compelling visual. And there's no rule that says thou shalt maintain the same order throughout all uh all columns. And you've got the add a complication that would be able to like actually render all the glyphs. You're not gonna render rectangles, but you could. And I guess this is like the this is the other piece of it. It's like you could you've got uh X1, X2, you basically have the four corners of a rectangle. You could just go render a rectangle. And if you're wanting to prove out that hey, this is how this works, you probably would go render rectangles. And then you'd go draw nice lines through them. So you maybe have some rule in there that the area of this line needs to be conserved or something. That might work. But sometimes it gets into the gnarly like the actual rendering details. How uh how tightly do you hit the point? You've got a lot of variation from column to column. Why don't we suppose to do with that? Like uh you should have the shapes be as reasonably reflective over the quality as possible. So that might be hard. Okay, that's all feeling pretty good. We've got a pretty straightforward system then for rendering the rectangles. And then we've got an extra layer that comes through and says, oh, we're actually trying to draw lines through all those points. So this is the second time that we've come up with this uh well, I need to go draw an arbitrary line through points that I know, but that are kind of hard to express. Like I want to collect all of the Y2 values and go draw a line. But then what do you do with the moles in the middle? I don't necessarily want to draw a line that connects those to think about that. But kind of the if you had to think about a kind of a primitive that's in there is that I've gone and computed my I've gone and computed the points for my like uh thing now I want to go render shape with lines and once you have the lines it's not hard to fill the inner area and if you've got nulls in there you basically need the lines to track to some baseline. I mean it's like you're adding zero um let's think about how that actually gets implemented. There's a couple details on there that I'm like hand waving that will become important. Alright what else do we have? Let's do. Let's do side by side bars. I've honestly never liked this chart. Um maybe it's safe to say it has its merits. What's really nice about the stacked bar is that it shows you how the parts contribute to the whole. What's nice about the side-by-side bars is they show you relative comparison of the pieces. And so it can be very helpful if you're wanting to make comparisons within, let's say a quarter. Checkout candy. Well if I stack them, it's actually pretty hard to compare the first and third categories because the heights aren't side by side. But if you can plot three bars side by side, let's fix it forward. You can just compare them side by side. And now I'm good to go. Problem is what you lose is you no longer get to see the uh stacked height. And so if what you want to understand is how does the total revenue of quarter one compare to quarter two? You don't have a strong visual of a sudden you compare totals across quarters. So I guess they both have their merits. Um other comment on selection is that for the stacked, excuse me, the side by side bar, you would often case be able to get away with well, in some cases you could get away with a just line chart where you're plotting multiple lines, one for each category, and now you can avoid rendering bars side by side. You avoid rendering a bunch of lines in the same width, but you could render uh you avoid rendering like bars inside the same width. Uh but now you're trading that for a bunch of lines that are possibly going through the same points. And if you choose to render the points that the lines are going through, it might get a bit messy if you've got a lot of similar values. Um to be aware of is that you could often take the same data and represent it as lines colored by their category rather than bars that are side by side. But at its core, it's the same kind of row column format as these other pivot tables. I've got time on one axis, let's say it's a column. I've got rows which represent the categorical contributions, so you get three rows by five times. I've got 15 values again. And so I got 15 values. That means I'm going to end up with 15 rectangles. And so now the question is for my 15 rectangles, how do I determine their positions? Well the bottom baseline is easy. It's zero. Top baseline is pretty easy too. It is just the value run through the scale. The scale could be linear could be logarithmic could be discontinuous. And so you run through the scale they give you your pixels and then you're off to the races. The trickier part here is that now we have this horizontal positioning that has a little bit more calculation going on. And so now in order to determine the horizontal position you want the bars to render with an offset so that the total available space for the column is split evenly amongst the bars and then the incremental horizontal offset is computable. And I guess the simplest way to do this and this is what you get if you're you would choose again an ordering on the categories. A, B, C, D, E, B, C, F, D, E. You choose your ordering. And now that basically gives you the horizontal offset. I know that this is bar number one, two, three, four, six. And so that gives me my offset. I know that my bar width is the available width divided by the number of bars. Probably minus one. And so there we go. No, not minus one. Two bars, 40 pixels, or 20 each. And so that gives you your bar width. The other way to think about it, so that one works as long as you're wanting to render gaps for empty bars. Now, in some of these charts, people choose not to render the gaps because if you've got a bunch of gaps or a bunch of moles, you're gonna put the goofy looking chart. And so you might choose to uh compress the gaps. This gets harder though. Because now you're either signing up for like some somewhere of a width has to now vary either. The width of the bars varies for the column. That's gross. Or the width of the cell, like the the width of the X varies with the number of bars because you want the bars to be fixed width. That also is gross. You gotta render the gaps, otherwise the things are gonna look terrible. Now you might choose to uh always order the bars so that the gaps are always on the right or the left, or you know, the bars being rendered for the middle or something. But I guess honestly, if you're gonna like choose this visualization, it's just kind of a bad choice if you've got a bunch of zero or nulls or something. Like what do you want to convey with this chart? You want to show the zeros, sort of the line would be better, and then you'll show it going down to zero. But whatever, people want to render everything. And so here the wrinkle is that the uh bar positions uh could be handled as accumulating. Um often these are done on categorical x-axis, is that right? And so it's like I already I already know the area that's available for the bar, and then I'm subdividing that. Um, a little bit different than the y-axis numerical scenario. But honestly, all these charts you can swap them around, and now you end up with a categorical axis on the y. I guess that presents a wrinkle of like you you definitely have to run things through the axis, the scaling map. I can't think through that in real time here, but like you'll absolutely render the wrong chart. It needs to be robust to handle uh an arbitrary like x scale, y scale, whether it's continuous or categorical. And so there's a right way to do that. I can't think of it offhand. Uh let's assume you get all that right. We're now rendering uh bars side by side where they're colored by this group. And so again, uh, we've possibly done a transformation to get the group total. We've then chosen this is a bar layout phenomenon. I want my bars to be laid out with this stacking arrangement, either on top of each other so that the pieces make the hole, or side by side so that they're available for comparison. And again, if you had to think of some common math on these, it's like we've got this idea of an accumulating layout. The position of the last point changes the next one. We can do a tree map. That's me. Hello. All right, so we can do a tree map. If we do a tree map, it's hallmark again is that you're drawing rectangles. Uh the difference is that you have some sort of a positioning algorithm that's running to pack them in. You're trying to split the available area so that it proportions are according to some value. Typically there's a color involved as well. Tree maps also often have uh hierarchical considerations, and so you're trying to render subcategories inside the same space. And so you've got small rectangles inside big rectangles. I just have categories I'm trying to tile a plane. I've got these rectangles, I've got one per data point. I go rendering. That's pretty straightforward. Once I get hierarchical, now I've got uh one per data point where I've possibly added in the parents because those may or may not be represented in the original data. And so if you need a parent to render as the sum of its children, you often have to add that in if it's not an explicit row in your data. But then if you're doing like uh pivot table math, this kind of thing is pretty common. You've got multivariable row selection, you're often reporting uh pivot table rows where the parent is the roll-up of the children. That works fine for like a sum or a count where it's additive. That tends to work poorly for something like an average, because now your average is possibly bouncing all around as you uh change the level of analysis, the granularity, the grain of the analysis. Um but again there's this idea of uh I've got my rectangles, I'm going to apply some sort of positioning algorithm to them. That positioning algorithm needs to be aware of possibly x scale, y scale, and then it goes and generates all of the x and y coordinates for every point. That seems pretty straightforward. And it's almost like there's an abstraction in there, which is I have all my values to be plotted. Now let me go run them through positioning. And it's not just that you need the X scale or the Y scale, it's that you're choosing a visual arrangement of glyphs that makes sense for the purpose you have in hand. But it's a somewhat arbitrary distinction. You've got you know these same rows and columns of data, but they can be rendered in 5, 6, 10, 15 different ways depending on how you choose to represent the value. Oh, that's a rectangle. Oh, that's a point on this plane. I'm now going to connect those points with lines. And there is still something missing here about how to define the line, because line definition is one of these just nasty things where you can always look at the data and choose the points to be connected, but then actually describing that in a spec-driven way, that's going to take some work. Each column then is its own scale. So you're choosing a scale for how to vary the y position, either numerically or discreetly. Then you connect the lines. Where you basically rendering all the dots, and you connect them one to the next to the next. And so here we're drawing lines where the lines are made by connecting the points in order. The variants are in an order, the axes are in an order. And so it's almost like you've got this X position that's being determined by this just arbitrary ordering of the dimensions. It's like I've I've created from whole cloth and X scale. The that's what it is. It's the the X scale is mapping the name of the variate to the X coordinate. And then that gives you the points, and then you connect the points with lines going from left to right, and then typically you color based off of some sixth variable, assuming it was five, some n plus one variable. And so you have a color scale to show. That's pretty straightforward. That's just the mapping. The y is determined by the again the variant, but here the variant is going and looking up a scale, and then you map it through. Alright, that works. But again, we've got points in order, and then we're connecting them. Alright, so this idea of points in order. It's like every line you've got to tell it what are the points, what is the order. Ideally you just supply the points in order. And then the line has a color, you gotta fly that out. So I guess parallel coordinates not that hard when you think about it like that. Um you've got y scales that are lookups. You've got an X scale that is a lookup, drawing lines, and then if you wanted to, the user could just arbitrarily move those y scales around. And that's honestly not that hard of a transformation. X is moving side to side, y you can resize the thing. It's pretty straightforward. And then you might choose, yeah, it's a good point. Um often on these categorical getting mapped into a coordinate system, you then choose to jitter. And so every y scale possibly has a level of jitter for each point. You'd probably find some way to uh some unique identifier hash it and then use that to compute the jitter, how it's stable across some seed. And in the language of D3 scales, you want those to be bandwidth scales. That way you're computing the limits on the scale, and then if you're just doing it simply, well, you just go render at the center. If you're doing jitter, you render between the edges with some randomness added in. Alright, what are the other uh charts that we've seen? Alright, we'll stop there. I need to go back through my uh index of charts and see which ones would still be very hard to position. But I think we're getting close.