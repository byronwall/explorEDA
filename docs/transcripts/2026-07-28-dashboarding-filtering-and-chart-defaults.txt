Alright, here we go. This note is going to continue the discussion on building an interactive data visualization system. Specifically dig into the features around I guess we'd call it dashboarding, but also the idea of user defined, user created, user configured charts. The idea that a user can self-serve the charts that they want to create. But whether it's a human configuring a chart or an agent configuring a chart. Tens hundreds of changes and discussion how many tokens you want to burn. Um but then this also gets into the world of the more advanced data visuals, so chart faceting, color scales, named variables and units, uh is the overall kind of consistent availability of things when you're working on some data visuals, and some of the interaction around filtering, digging in, and clicking clicking through, maybe basically what can you do if you've got this sort of top layer that is data visuals against some tables. What can you show the user on the first click? Pivot table show details as one example. Clicking on a cell can show the relevant data. Very often this data is connected to other stuff, and so there's a whole world of interaction that you can drive if you know something about what the data is therefore to put a finer point on that one in Shifty main visuals are aggregating analysis results over time series data. So take a two-second shift, compute max engine speed, you know, have a table of max engine speeds. But you always have the ability to interact with the chart, select a row, maybe filter it on scatter chart, and you get four or five of them. Then you can open up a view, which would go plot the raw time series data again. So you're constantly bouncing in and out of this is an aggregate, this is the raw data, this is an aggregate, this is the raw data. But you were looking at the raw data for the aggregates on screen. And once you have the ability to kind of flip timescales like that. Most important thing when you're building these interactive systems is it must be the fewest number of clicks or interactions to get something useful on screen. And so if you've already got a data context, let's say, I know I'm looking at this data. From there, you want to make it as easy as possible for the user to create the charts that they want. And so you want to make it so they can just click a button to get a scatter chart. And then what are the defining features of the scatter chart? In X variable in a Y variable. One might argue that if you just had an X variable, you could plot it on its own. And that might work. But you do better with two X and Y. And once the user has selected X and Y, you then show them a chart. If they only give you two variables. But if you know just a pinch about what they might want to do, or even if you don't know that, if you just know what is likely good ideas for a scatter chart, you can go make a chart that's useful right off the bat, and then it immediately invites the text and the second and third clicks to dial in the configuration so for example you create the scatter chart they know that their data has a couple categories in it maybe you're plotting I don't know uh let's just keep it in transmission land uh engine speed was always important uh temperature was always important our flow rates were always important so maybe we're plotting a flow rate versus temperature trying to understand how fluid viscosity affects something or how something else affects something so you get your plot and you see what sure it looks like a couple different lines in there sometimes you're looking at your chart and you just know that there's categories and so in this case engine vehicle range transmission range first second third fourth reverse super important and so then you're looking at this chart and you ask the question and how's this all vary by range and so they can click settings. They can choose a color variable. Range is a choice. The chart instantly gets colored by range. And then they can really quickly decide if that was useful or not. And so that whole flow of making a scattered chart that's segmented into series like this. Once you've done it, it's three choices across maybe four total clicks. And then getting back to kind of config. And so part of why this works is because you in advance make a bunch of good decisions about the rules for determining the defaults. So what are the things we've had to determine? Most importantly is going to be the scale limits on the charts. To be the same for the same variable. Like you want the default to be the same. If the user goes and overrides a chart, it can behave differently. And so the default that you want is for the scales to be determined by the min and max of all available data. And the reason why you do it this way is it makes it so that if you're doing brushing or interactive with filtering, the charts are not by default changing the scales as you filter. It's incredibly disorienting and honestly misleading until you know what you're doing to have chart scales change just as you're filtering the data that's displayed. And so this gets to another important concept, which is it's called the working data. The idea of the working data is that there's some larger data set that's available to the client right now. Maybe it's uh 10,000 rows, let's say. Uh via some sort of like request or boundary. So in Shift E, the working data was given by refreshing the page. And I had the idea of what I call server filters. So these are filters that applied before you ever got any data to the client. Once they were applied, the client had no idea what data was missing. The goal on server filters is to make it so that you could maybe have a data set with a million rows in it, and then filter it to the 100,000 that you could actually work with. So that first chunk does a filter against all available data. Client is only aware of the filter it sent and the 10,000 it has. And so I'm gonna call the 10,000 rows the working data, which is to say it's possibly some subset of all available data, but it's the data that the client is fully aware of, but they may not be fully presenting, because again we've got client-size interactive filtering. And so at any given moment a user can click a bar chart and filter by a specific value, and we drop from displaying 10,000 rows to displaying 1,000 rows, and so there's this like constant tension when you're in these systems of given that I've got at least like three levels of available data, all data, working data, visible data, against which subset do I do all of my scaling and other math, because when you pick one or the other, you're basically kind of choosing the experience you're gonna have. But on every chart, you give the user the ability to auto-scale or rescale on filter. Probably find a piece of name for it, and when that box is checked, you're switching the default scale map. So instead of it working against the working data, it's now getting them action and in from the visible data, which has the effect of now some of your charts will be readjusting, resizing as you click around and filter. And the reason why this is so important is once you understand how this system works, it gives you the ability to designate some of your charts as almost like working charts. But when you filter on it, it's brushing the visible data. And now these charts that are auto-scaling they update. Then have to choose how many unique like islands of data are there. And this gets really gnarly really fast. So in GIFT, my rule was that there's only one island of data. Every chart on the screen is backed by the same working data and visible data. And so if you set a filter in one chart, it applied to Google page. This removed the possibility that you might scroll down across some boundary and then be working on a completely different set of data. This added the problem though that if you were trying to do three or four things that legitimately needed different subsets of data. Well, I made that harder than it needed to be. Save your current view before you switch to views, you would lose all of your in-plane changes. So a much better system would have been to almost create tabs or some sort of like multi-document kind of viewer where you can keep dirty working changes. You can go spawn a quick view of stuff, you can make changes, you could save it, you could spawn a new view which has already been saved and load that. But this is the idea of when the user is filtering their data, do they know that they're filtering? And to the extent they've set some filters, can they understand where all the filters are? This is another one of those things that was uh eventually got pretty good and shifty, but in the beginning, it was not very good. And so let's dig into how could you control the filters. Well, first level of filtering was this idea of the server filtering. So as I'm going from all available data to the working data that the client can see, what filters are being used there. So you'd click the button, it would say, like, I don't know, reapply server filters or something. Go do that. And there was a display at the top, which was here's the active server filters. So these are the filters before you get any data to the client. And what was by far the most confusing scenario is if you accidentally configure those so you get no data. Um you had to like show this other screen how your server filters are such that you've like filtered everything out. So that's always confusing. So you get these server filters. Changing those was not very easy. And since you control the filters at like runtime, you would just open the chart. And obviously the easiest way to control the filters is just to click and drag. And so like the primary mechanism for filtering was to click and drag on like uh bar chart, single click on a row chart, uh brush a 2D area on a scatter chart, click on a box plot, etc. Brush on a parallel coordinates plot. And so that's giving you this really fast, smooth way of applying these filters. But the problem is that you're trying to then go make sense of what are all of my active filters. Well you've maybe clicked on five or ten things, and some of those clicks may have no impact at the moment, or they may have a huge impact at the moment. Once these filters become additive, a change you made at step two might be invalid at step three. Is that true? I don't think that's true because they were replied as an or. Excuse me, they replied as an and. So you're always narrowing basically a funnel. I had 10,000 rows. That filter got me to 5,000. That filter got me to 3,000. That filter got me to 200. Now if you were to go back to the top and remove one of those filters, you might jump to a value you've not seen yet. Oh, and I clear that one at the top, but I keep the other ones in place. And I'll go back to 5,000 rows. I go to what would it be? Maybe 3,000, maybe 8,000. I think both are possible. It could get bigger or smaller once you start popping filters off the top. But seeing that uh stack of filters is very important because it lets the user understand how they get from their working data to their visible data. Alright, so the next consideration is what are the other mechanisms to filter? Because it's not always the case that you can just click and drag. Sometimes there are like explicit filters you're trying to hit. So one way to do that is to just uh provide input boxes. So a scatter chart has X and Y variables. If it's numeric, it has men and max. You can show them in, you can show the max. Let the user type in some values, hit update. If the user is brushing or dragging them around, you update the display. You also update the input fields. Now the user's got two nicely synchronized ways. This kind of hints at the problem I was having, which is all of this filtering stuff is very chart specific. You're interacting with a chart to set the filters. But on some of these big dashboard things, you may have charts that span three, four, five total viewports. People would put together some crazy stuff. And so now it's really easy to accidentally have filters in place that you aren't anticipating. So I added a dedicated view, which was just a summary of the filters. I guess it's worth noting before we get too far that all these charts were embedded into a grid where the user could drag them around and resize them. It's basically like a masonry style grid or a dento style grid, something like that. It's using React grid layout. So they could arbitrarily position, resize these charts. And then it switched to the arbitrarily and reposition these just content elements. Some of the content elements were text, some were uh various chart types, and then some were these interactive things, like a filter summary. And then it was responsible for setting setting or viewing the filters. And then this gets to one of the most confusing wrinkles about how the cross filter library works. Alright, so here's how it works. If the chart you're looking at is filtered, it behaves differently since it created the filter than a chart that is showing the same variable, but was that responsible for the filter. So the way this worked in cross filter was the chart that created the filter. When it renders, you would get a different uh it would apply the filter, but instead of just excluding the rows from the result set, it would keep them, but it would make their count zero. I think that's correct. Or it would somehow give you the rows, and it was on you to determine that they were filtered, and then to choose whether you wanted to render them or not. And if you rendered them, what styles? And so the reason this was done is that you could make a scatter chart where you draw a filter, and inside the brush region, where it's all filtered, you draw the points like normal, green, blue, yellow, etc. But then outside of the brushed region, instead of just hiding everything, you instead could render them at a gray, maybe slight opacity change. So basically on that chart, you're getting this really clear visual of oh, these are selected, but these are available. And so you look at that and you say, that's actually really nice. Now we're getting really confusing is in cross filter, it has this idea of a dimension. And so if you've got a variable like engine speed, you're technically creating a dimension called engine speed. And when you're filtering, you're technically filtering your engine speed dimension. And this is a choice I made, but when I was making all of these charts, I created new dimensions for every chart. Which is to say, if you plotted the same variable in different charts, they go scatter chart here with engine speed, they got a scatter chart there with engine speed. It was set up so that every chart had its own dimensions, which meant that when you filtered one of them, only the chart where you actually applied the filter would get this special logic on oh, these points are gray, these points are something else. Now this is the choice. So if you've got a big panel of charts showing the same X variable, one of them would be setting the filter, the others would just be showing the results of that filter. Which then means you have to choose whether or not to show the brushing elements on every chart. You could, you could not. But then it also means that when they go and collect their data, you're going to have rows available that have been filtered out, and it's on you to handle all the logic of removing them. And where this gets really gnarly is when you're starting to do math on the data. Because now your math can be completely wrong if it's incorporating rows that are hidden. Boxplot is probably the best example of that. Box plot is rendering the result from the transformation using these aggregations to get the median quartile ranges, etc. outliers. And so if the data coming into that thing needs to be evaluated for, oh, are these actually live right now? You put a bunch of complicated math into every chart rather than just letting uh the chart worry only about its filtered rows. Okay, then why is all that matter? Well, it gets you to the spot where you could technically filter a variable multiple times. Because if you had three or four different charts that were fine engine speed, and you set a new filter on each one, because you basically know every chart is only seeing the data that the filters have made available, and so you've got less data on screen. But you may have had an initial filter that was zero to 3000. Hey, it's just anything outside of that's bad day to get rid of it. Then you might say, Oh, I'm looking at the specific scenarios. Now let me filter a different engine speed from 1800 to 2600. Because now you've got two filters on engine speed. And if you go and remove the top one of zero to 3000, nothing happens to the visible data because you've got a narrower one below it. But then the chart that's setting the 1800 to 24, 2600, it now will look different if rows are coming in better, you know, it will now see stuff that's outside of 3000, possibly, because you've removed the top-level filter. You might end up filtering two or three times. And I think what was missing, and this is just like a UIUX problem, was just a clear visual of here's all of the filters that are being applied, here's the filters that are affecting the same variable. Um for numeric stuff, you could definitely get a really nice view, like this exact sort of like funnel sliding range thing, um, and it'd probably make pretty clear what's going on. But then being able to jump to the chart that's setting the filter, I think was a feature I always wanted but never added. Because especially if it's three scrolls down, uh, it might be hard to go find it. And then especially if you've got some of the settings set up so that it's um zooming on filters, uh changing the scales, it might be really hard to go find the chart that actually sets the filter. Um but all in all it worked really well. I think the ah, the the one other thing that I should have separated is when I went to save the view, I saved all of the chart filters alongside all of the other view details, which made it really really difficult to get like a common view where you could then go change the filters. And so then if you were a user who is not doing this multi-dimension, let me pop the same variable twice kind of stuff. You have this a little bit weird experience, which is it's like, man, I just want to create my charts, and then I just want to filter my variables. But I didn't make it easy to have this like top-level filter, um, at least not in the beginning. Now I eventually added that, which is I added in a top-level filter component, and it did its own sort of trickery. It just created a new dimension for all the variables that you asked to filter in. But it worked perfectly because now technically inside that filter component, you could get access to the uh active inactive regions because that was the dimension that's at the filter. But then I didn't give any visuals for that, so it you can actually do it. Um, and so instead the effect was that I created a new dimension on the same variable, which means every other chart looking at that variable just gets the hard uh clipping. It doesn't see active inactive, it just sees oh there's only 300 rows instead of 10,000. And so then those top-level filters were uh increasingly the nice clean way of making all this work. Um I should have added them way sooner. And then that sort of let it play nicely with well, now that top level filter is its own chart component, it's just in the grid, you can put them anywhere. You could technically put multiple top-level filters out there. Um they would since they're both creating their own dimensions, you'd have that interaction. But most people you know didn't do any of that, and so it all just worked. Um yeah, the missing piece that I never implemented was to disentangle the view saving from the filtering, and then to allow the user to create multiple views with like a tabbed interface where you could just keep multiple things open at the same time. I think maybe in the beginning I was concerned about uh just overall performance. But then also I was concerned about there going to be questions around hey, what data am I actually looking at on the screen, especially if you're making views that have similar like dashboards? I mean it's one thing if you like have three views and they're completely different. Um but it's a different thing if you've got three views and they're all the same base template, but you're looking at different scenarios. All those problems uh could have been solved. Um, but it kind of hints at to give like okay now an overall description of how do you go from uh all data, working data, visible data. Uh clearly what's going on there is you've got a bunch of filters available, and so uh being able to control the filters smoothly, cleanly, and really separating the runtime interactive filtering from the it's called the real deal data filtering. That's one of those things I did not get right. And so in the beginning I forced all of the real deal data filtering to be applied using the runtime dashboard filters, which then made it kind of a confusing mess if what you really wanted to do was both. I want to apply this top level filter, and then I want to do my interactive brushing. Alright, what's up there? That's probably enough.