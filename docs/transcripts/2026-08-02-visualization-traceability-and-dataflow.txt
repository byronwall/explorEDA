Alright. There we go. This note is going to continue the discussion on interactive data visualization systems. And specifically specifically reopen a line of discussion just to talk a bit more, put a little bit more color on the idea that we want complete traceability all the way from the source data to the glyphs that are getting rendered. And yeah, let's get specific. Why is that on my mind? Well, I think this is one of the areas where uh ggplot approach really rubbed me the wrong way. And in particular, I always found it confusing or frustrating the way ggplot would compute distributions, densities, and other related things. And basically it was a little bit too uh presumptive about here's the path you want to take from your source data to the thing being rendered. And that often manifests itself as needing to jump through hoops to either reproduce most of a calculation and then make a change, or needing to really dig into how something was being put together to confirm that it was correct or to confirm that it was incorrect and at least as far as I'm aware back then it wasn't really possible to get a clean diagnostic from we've rendered this density line over into and these are the points that influenced it and the reason why this matters is because very often you would be feeding it three four five variables and you would be faceting and building scales and pretty quickly it's not clear which subset of your data is being used for a given part of the graphic. So for example when you tell it to generate a color scale I'm pretty sure its default was to generate something on the global color scale but then it wasn't clear that's what it did. Maybe it was generating a local color scale now you get six color scales you have a facet you'll think it was doing that though. Or in other cases, you're trying to get a line on the chart, some sort of threshold line. Maybe this is like a process control chart, and you want to see some standard control limits plus one, two, three standard deviations, that kind of thing. But you're wanting to facet those by some measured variable. This is melt flow, this is density, this is something else. But then you want the upper lower control limits to be drawn from either only the data associated with that variant. Or in other cases, you've pre-computed some table of limits, and you just know in advance. Maybe in the worst case, you want to do upper and lower control limits. But then in a handful of cases, you want to override it with known annual values. And this is repeating over 6, 9, 12, 20 charts. It's a real nuisance to try and make sense of what is this thing doing. And especially that idea of I want the data to inform some sort of glyph that's on the chart, but not like the core glyph. So it's like I want to draw a scatter plot, and then I want to draw a line. If you had X, Y aesthetics. And then you could technically fly new data in, but when you chose to fly new data in, it wasn't clear if it was keyed the same or different. Alright, so we get this idea of wanting to fly N specific lines, geometry shapes, and for those to be derived from the data with sometimes non-trivial transformations. And I really want there to be kind of like a first class parity between some user supply transformation and the built-in transformations. Like if we're going to go compute some 2D density, that's fine. But that thing should show up basically the same as if the user is supplying some sort of density transformation. Because at the end of the day, internally, you're going to go generate some estimate of density, and then you're going to go turn that into contour lines or colors or whatever. Something, which is the density. So being able to see that thing would be nice. Seeing how the density updates. And then this is its own sort of like using data viz to explain data viz. And you do that a couple times. But even if you don't do that, it still is just like a really compelling visual for somebody to go make as they're trying to like make sense of the graphics that they're making. Or the land of media. Detailed infographic that's been faceted. And it's just sort of assumed that it will only work if you feed it data that's already been constrained by one of the variables. It's a really nice sort of thing. I feel like maybe it was uh visitor numbers over month over time. Something like that, it's something kind of cyclical. But I had this very clear correspondence between you had one of these charts and you had a single national park. The thing didn't make sense if you rendered it with two national parks worth of data in it. But then I'd be curious about something like that. How do you set it up so that you've described this single chart? And then once you've got this rich single chart, you can then say, I'm gonna go split this data set and feed it into that chart. Because that chart was not some standard scatter bar, etc. It's like that chart is its own kind of complicated mapping. You give it data of a given shape, and it produces all these lines and things. But then at its core core, it's just like a polar coordinate and a line and some dots and some text. And so it should be possible to map how the data coming in goes into all of those points, even though there's very likely some transformations being done on the raw data. At least that's my guess. And then this gets back to one of the other just core tensions and all that. Sometimes you know the shape of the data you need to generate the final visual. And so then you go and split the data processing from the visualization. And basically step one takes your raw data and puts it exactly into the shape you need to then go draw the lines and boxes and stuff. And I think this is one of those uh I want to call it anti-patterns that I really want to try and work against. And the reason why it's an anti-pattern is because at the point you were doing the visualization, you're now required to do a bunch of work to make it so that if you click on a series, a chart or a point or something, you basically have to like rebuild all of the data processing inverse so we could trace something back to its source. Is that you're describing the full suite of transformations in basically one object? Like how do we get from raw data to the glues. All right, so again, the ideal is this when you're starting with source tables and you're eventually ending up with circles, lines, paths, whatever, I want to be able to click on anything and trace back any and all transformations to get it back to either a source table or some fixed input table, like a mapping kind of table, which again is just a different type of source table desire is that when you're going through an aggregation, you keep track of enough information that you understand how a index or key on one side maps back to the source data on the other side. And if you do this basically at each step, kind of by definition, you should be able to work your way back to the raw tables. And then I think where this heads is if you're executing SQL, you get some sort of query language that's actually generating the source tables. You can then see how the queries are either working with or against the visualization. Got all these tables. Five six tables. Here's the five, ten, twenty fields in each table for each one. You can see how the fields are flowing into the eventual graphics. Okay, and so in practice, what does this actually look like? I'm picturing some of the more advanced data visualizations that I generated. And I think a common theme that comes up is when you're fastening or splitting and you're wanting to show group or facet specific lines because this is one of those scenarios that's like pretty easy to describe and it's really not that hard to do if you're doing it in Excel or something. It's like you've got some raw data table you're filtering, you're now looking at subsets of the law table, you do a little bit of math to generate the chart, and that all feels pretty good. But then when you go and do it for the four by four grid of all of them, and you have available to you the whole data table, or you could like render the sub data tables, I think that's the that I really want to like see played out. And I guess yeah, what I really want to see is what's the uh catalog corpus of tables, what are the tables look like at each step? Because clearly I got 10 source tables, some of them are just not going to get used at all for certain charts. It's like if I'm asking, hey, for this chart, how did it arrive? The very first thing that's gonna happen is there's just some tables that just are used. And so then my first cut is to just threw out those tables. Yeah, we got them available. We don've plotting them. Next. If I've applied some sort of filter, so that's of interest. It's like I'm now inside of a filter boundary. Filtering reduces the available data. And then it affects every chart inside that boundary. So if we're thinking about the old shifty examples. And so if I wanted to understand how my charts came to be, I'd take the raw data, apply the filter, and now I'm looking at the available data. But what I always wanted to do was to give more control over these almost like islands of filtered data. So that's one version. The other version is like islands of I'm plotting this source table. So in Shifty, even though you have three or four kind of sources of data coming together, you're not really have the ability to like independently plot any of them. then just want to put together a plot of some file attribute versus some other file attribute you're very liable to get the wrong result because what's going on behind the scenes is it's double tripling it's over counting the file instances depending on how many events were in each file and for something like a scatter plot maybe it didn't matter because everything's been plot in the same spot and you can't actually tell that there's 10 points in one spot. So something like a bar chart is absolutely broken because now your aggregate count of some file attribute well you're counting events with that file attribute counting the file attribute on its own. And so that's a problem which is that if I'm wanting different portions of the visualization to be based off completely different source tables. I want that to be a supported thing uh hey you can just create arbitrary glyphs on the surface from arbitrary source data if you want it to be a scatter plot perfect here's a scatter plot of this data it's gonna render in this spot it may or may not be affected by global filtering and other stuff. Yeah I guess that's the point is like I really want to have much greater control and visibility into how various global or local parameters are affecting the final displays with the idea being that you can take any individual chart and go reproduce it in isolation with all the same correct stuff. Like I I want to have kind of complete knowledge of every step and transformation that went into generating the final visuals. Because if it's um three, four, five, six, ten complicated chart making an infographic with 10 things on it, well maybe what I really want to be able to do is make each of those charts individually in isolation and then go weave them together in the big picture and then some weaving them together maybe then I'm integrating and wiring up some of these global filters and I feel like this sort of thing has a good bit of play especially if you're looking at like a uh I don't know sort of like these multi-scale dashboard type visualizations. Imagine a core plus map of the United States is showing you some just global trend well national trend and you click on the state and now you're getting like a state level dashboard now that you've clicked on the state you've seen a zoomed in core plus of the state. You're seeing some charts on the side that are cheap state specific. And it's like depending on what your scale you're at, you get slightly different charts. But if you wanted to go generate the exact same chart, let's say on the server side with no like client interactivity, I want there to be a super clean spec, which describes the exact sources and transforms, uh glyphs, etc. And how they came to be. Yeah, I think that's right. It's like you want everything that depends on the facet keys to be like explicitly handled at the boundary. That way, inside the facet, it's only concerned with rendering the data that it's given. It's not having to make logic choices about what facet am I. Probably a reason that wouldn't work in reality, but that's kind of my mental view of it. Like you want the rendering inside the facet boundary to be super clean. I don't want every facet chart to be checking all data and then making decisions based off of its acid keys. Because then this leaves it up to me. If I've got this generic kind of chart container, well, I can go feed it data from whatever, you know, whatever mechanism I want. Most likely mechanism is going to be some sort of a loop. Specifically this idea of go identify all unique keys. Give me a chart for each of. So it's like a for each kind of thing. So that's a super important primitive, which kind of couples a filtering with a handoff. Is that right? I have to think about that. Because again, if my goal is for all of the transforms to be done kind of at the boundary, well, for a non trivial chart, you may have a couple transforms that we could be done. Well, we didn't give this an example for that. Um okay, then this idea of a spec gets to the next thing, which is just like a big big picture detail, which is that I really do want the rendering to be capable of being done completely server-side without any actual rendering capability. It's like it should be possible to get the exact same chart from uh a real canvas implementation from a server-side uh drawing library from actual SVG. And I want the description of the chart, description of the data, description of the operation, description of the rendering pipeline. I want all that to be fully abstracted out so that you've got this chart data pipeline that's just being connected to a renderer. And so then if I want to go to Canvas, I'm fine. I want to go to SVG, I'm fine. That's fine too. Most likely reason to do all that would be performance. I want to go to multiple canvases. I want that to go modify some part of this chart data graph. And then it renders and updates the according the pieces accordingly. What that sort of lends itself to is almost like some sort of uh diff type operation where it's selectively choosing oh these need to go get re-rendered. Although I guess on most brushing operations you're very likely going to re-arrender the whole thing, at least on the initial brush. But maybe it's smart enough to do all that that we can get really fast incremental renders, especially on canvas or something, where you can blank out sub regions. Okay, so we could have this primitive of a uh facet, which is like some sort of uh uh loop do multiple times kind of mechanism. Um then one of the other like big primitives is this idea of basically like a parameter. It's like here's a value that I know everything depends on. I might choose to derive this value from something in the data or something from the user or something from the environment or runtime. Like what is the width of a chart? Well, it could just be a number. But then more likely you're gonna know that the viewport is some such dimension, you're trying to render four of these things, and so how's the width determined? Well, in this environment, it's actually the viewport divided by or uh sliced up by the interactive grid layout that's out there, and so I'd want to be able to see that chart width is one of these parameters, and that in this environment it's being driven by the viewport, but maybe in a different environment it's being driven by some declaration of this would be 800 by 600. What else is a parameter? Well, if we get down to the individual individual glyphs, you've got a single circle. A radius, diameter, or size, has a color. Color could possibly be the stroke, could possibly be the background. You're gonna put a stroke on the thing. And so already we've got uh five six values there. Every data point could be labeled. So you've got text. Uh text probably has some uh offset relative to the point. Once you're offsetting text relative to the point, you might then choose to do a leader line. So now all of a sudden, this simple little point has ended up with 910 pieces of data associated with it. And each of those pieces of data is going to be driven by either a constant, some internal calculation, like a heuristics, heuristic or preference, or it might be driven by a scale. So X and Y position are nearly always driven by scales. Well, it's a scale. A scale is the transformation. It's taking some desired set of input data and mapping it into pixels, or taking some input values and mapping them into a color, possibly. Mapping a domain of things into a range of things, where the domain can be continuous or discrete, and the range can be continuous or discrete. Although I guess it might be difficult to map a discrete thing into a continuous thing. Okay. And so we've got this idea of these scales. What's common scales are going to be numeric scales for positioning? X, Y. They might be linear, they might be logarithmic. What are they given? They're given they got some incoming units. Hey, what's coming in here is RPM. It's gonna go from zero to three thousand. What about that to map from pixels? Going from zero to height. Or more likely going to some kind of abstract. Well, it needs to be the chart area minus some margin, minus some padding, blah blah blah. So it's not actually zero pixel to 300, but there's some sequence of things being applied. If you were super serious about it, you could show the full logic and math for all of that. Well, here's the zero point. Here's how we've chosen where the scale actually goes to and from. basically the range of the X scale, the range of the Y scale. You can show the padding, the spacing, the other stuff that's been put in there. So it still works. We've made room for the ticks. We've made room for the label. We've made room for a title and a subtitle. We've made room for a legend. Blah blah blah blah. Alright so we've got all this stuff. And so then if I'm trying to look at how did that individual point get its X or Y position. I'm gonna see that that point is associated with a data point. You know it's got some index data index. This is the fifth, six, ten, twentieth point. I think it leads to some sort of lookup into the source data. So kind of right off the bat I'm already in the land of while this chart is being made from that source data. I've got this table of values given that I've got the table of values I can show how each data point is mapped through or renders. Alright so that's pretty straightforward. And then maybe the scale is computed from those values and so then I would show that same table going into the scale maybe the scale is determined from the global max and min values and so maybe we've got kind of the same variable being used twice. Once with a global set of values, another with like a local filtered set of values. That's real common on a facet where you're either computing scale limits based off all data. And then rendering based off some local thing. Alright, so what else do we have? We've got a whole slew of colors and size decisions. I think all those are parameters. They're gonna be motivated probably by some sort of theming system. So what does that look like? Well, I've got some parameters that are used to determine size fonts colors. So all of the typical defaults. It's not that they're defaults in lines of code. It's that they're default choices in this execution data flow graph. But then once I know that they're there, I can just freely swap out a default for some other value. Now we're kind of in the world of well in the system what is a scatter chart. And it's this big graph where at one level you can just see some simple choices. X, Y, color. Go pick your variables. And so it could be as simple as three choices. But then in reality, what do those three choices actually expand into? Well, we're gonna take the X value, we're gonna find the max and min, all of it. We're gonna take the available plot space that's given by this box that we know it's drawing in. We're gonna take uh same thing on the y, same thing on the color. And then we know you want a legend, so we're gonna start that out in the bottom right-hand corner. We maybe make it dumb at the beginning, and it just overlaps, and then it's on the user to go and resize the legend. And so then it's like they are adding in some overrides. And one way to think about it is you're actually like adding in an override node. That's probably easier to think about in terms of uh visibility, undo, redo, all that sort of stuff. But then they've got this override that affects the legend that changes this position. Boom, done. You get this big facet. For some reason you want to do something special in one of them. You in theory could go expand the whole facet and then go target a change on just one of the graphs. Alright, say more about this later.