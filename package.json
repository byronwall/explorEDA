{
  "name": "data-viz",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "check": "pnpm --recursive --if-present run check-types && pnpm --recursive --if-present run test && pnpm --recursive --if-present run build",
    "release": "pnpm check && changeset publish"
  },
  "dependencies": {
    "@changesets/cli": "^2.28.1",
    "prettier": "^3.5.3",
    "turbo": "^2.4.4",
    "typescript": "~5.4.5"
  },
  "packageManager": "pnpm@11.9.0"
}
