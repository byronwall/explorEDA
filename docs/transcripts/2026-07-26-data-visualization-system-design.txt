Alright, here we go. This is going to be the first in a long series on data visualization. Figure it's about time to get my thoughts on Dataviz on the table. I must have been holding off on walking through data viz. Because I'm pretty confident once I start discussing it, I'm going to want to go build the system that's described. And I've kind of held off on building it since it intersects somewhat closely with what we do at work. But I think another read is having well-defined thoughts and some experiments, even if they cut close to what's going on at work, they're still worth pursuing. And being honest, I'm unlikely to get to implement things exactly the way I want at work anyways. And so I may as well describe what I want. And so it's going to take a bunch of different notes to kind of walk through the whole system, the highlights, all the different chart types, all the different interactions, all the different touches and flourishes that make you feel good. And on the one hand, I'll be walking through all of the stuff that got created for Shifty. So it's a really thin, mediocre version of this that got made for that Explore EDA thing. And I think unfortunately my skills weren't enough. The agents weren't good enough. And I didn't have a clear enough description of what I wanted to do back then. But I'm very confident that so I wanted to take another run at it. It's well within reach. So where to start. Let's start with a very high-level overview of how the system works. And then I'll talk through what I would like to see from a data tracing point of view or an auditability diagnostics point of view. That I think has a ton of promise, and that was never present in any of the systems I built before. And unfortunately, you kind of have to consider it at the start in order to weave it through. Okay, but super high level, do we have? What's the input? What's the output? The input is some source data, and the simplest version is a single table. In the more complicated version, it is multiple tables that have some relationships between each other. What's the output? Ultimately, the goal is one or more data visualization artifacts. So those can include charts, like a scatter chart, tables, like just a list of rows, uh single values like a metric tile, and you have this kind of dual that if the output or renderer is interactive, which is so you can click on it, brush on it, zoom on it. You get this additional interplay where the output loop stack around and can drive at least the display of the visuals, but in other cases it can drive the inputs as well, especially when you start talking about advanced filtering schemes. And what's in the middle there? Well, in most cases the data is not exactly in the format that you need. Some of them are data normalization, so we've got the missing values in some cases. Some of them are mappings and lookups. Maybe we take a customer ID, use that to go look up their name, because we're starting with the orders table. So unit conversions are an example. Although I guess unit conversions are a little tricky because ideally you'd handle those automatically. If you had a first class unit system. But assume you don't. Now you have to do math on the spot. What other transformations exist? You might choose to do fancier things, like a principal component analysis, or train and then apply self-organizing map or psalm. Those were features that were available before. You might choose to compute some aggregation tables, or at least some aggregation results. So imagine you've got orders, and you'll have to go determine what is the rank of this item in the total orders per month, something like that. You might group by month, accumulate or sum on uh order value, which is maybe the quantity times the unit price, and then you would go sort all those values. You go look up the current rows value, and you end up with a rank for every product. You know the product's month for the current row, you know its value, so you can figure out the ordering. Um so a lot of this stuff, one of the kind of like key questions is how much of it do you do at runtime, how much of it do you do in advance, and basically materialize or store the intermediate tables over on the side. Reasonable question is are all of these intermediate tables available for introspection or are they merely calculations? I know when I was really shifty, I actually did it both ways. Here's the ability to add a pivot table to the visuals and its mass was separate from if you just add a calculation which was an aggregate sort of thing. Not sure one's better worse than the other well actually I probably am sure you probably want to do the math one time and then choose whether you render it or not. So there's your answer. What else would you do any calculation? A lot of uh date processing extract the month extract the day a lot of string processing split concatenate that sort of thing what other calculations max men maybe again you sort of get into the world of aggregate kind of things you might choose to go compute and aggregate and again you get back to this idea of how does it attach to the original table where is it stored and repeated yes you could do it either way for reference the way shifty did it was it calculated if it was a single value calculate it and then repeat it down the whole table I think it was pretty inefficient and how it chose to do that that's pretty much all right so another question uh on the just complete input to output mapping is if you're attempting to handle arbitrary data of multiple tables you need to have some system to explain the relationships shifty was always kind of able to cheat because it was doing this uh multi-table visualization but the tables were a fixed schema and so I could kind of encode in advance all the relationships that's fine but then often it was uh you're working against the user sometimes maybe they're trying to add a relationship. Pretty common example is hey, we uh had this static table over on the side. We computed I don't know, some detailed value. Maybe it was uh max allowed engine speed by range, and now you're deciding uh to do some sort of lookup on that table, and those sorts of things are always a mess to try and bring in a calculation. I guess depending on where in the pipeline you want to bring that calculation in, it was uh either difficult or impossible, and then uh there's just no like first class static table offering, but you can easily imagine if you're in a multi-source multi-table paradigm. Well, that's just another source data. You've got range in there, one to two, three to four, eight to seven range is just an index that can be keyed off of your lookups. And you could easily have this piece of data available for any range. Here's its max speed, and then if you're in a chunk of logic that's there's the range, well, here's a piece of data you can pull across. And so I guess that kind of touches up against something you have to face when you're doing these data visualization systems, which is how much modeling capability you want to give the user, and to the extent you're gonna give them modeling capability, which is to say this is related to this, is related to this. How do you then expose those relationships in the data visualization? And again, to point back to Shifty, it's one of those things that it made very difficult. And often you were sort of uh forced to understand the internals of how it worked in order to get the correct answer. That was maybe a little bit by design, or at least I knew to go build a whole modeling system was a different endeavor than what I was trying to build. But very often you would uh actually want to do some of these modeling tasks where you can faithfully bring together two, three tables and go make a semantically correct pivot chart that is polled across the relationships. And I guess the reason why I have never really harped too much on getting it correct is you can always produce the denormalized table that has all of the data in it. It's inefficient and it has a lot of repeats, but you could always for a given chart express the data as a single set of rows and columns that would give the correct answer. And so, since that was true, I always just cheated and forced the user to go produce that single table, and then they could go visualize it. I think the area of shifty that made this the most annoying is depending on the visualization you were doing, it was sort of implied that you were looking at all collective events. So imagine there's 10,000 of those, but you might know in advance that the thing you actually want to go look at some subset of those. Because maybe what you're really interested in is some visualization that's just the uh available ranges, and so you've only got a cardinality of nine or sixteen or something, and so you have to do a bunch of work to get it so that you were kind of working with those nine or sixteen items, because internally I was wanting to process all 10,000 rows. I guess this is but one example, and so I guess what I'm describing here is that there's the input, which is describing all of the data. So I have these tables, they contain these columns. I have this many rows of each. Or really what I wanted to do was go produce a scattered chart of I don't know, some join or some cross-product between two tables, and this should have a million points, not just 5,000. So you've got like the data, which is to say all of the available things that you could manipulate to produce the actual tables you're looking at. You then have the raw tables of data that are coming into the visualizations. So you can imagine a million rows, you choose to filter. Now you only have 10,000 rows, and then you choose to plot 10,000 rows. And this starts to get quite complicated because depending on how you do the filtering, you will arrive at different results when you start doing aggregations and other things.js cross-filter style filtering. I want to treat it as though these other rows just simply do not exist. For example, uh pivot table is inherently doing its own set of transformations to identify the unique keys, compute the aggregations, and then display the results. And so either in the pivot table or external to it, you then have to choose how to represent that uh how to represent that data. Um, and so then what you've got is depending on the chart type, it is often implying some additional transformations that are going on. Uh start with the simplest case though, where there's no transformations. You've got a scatter plot. Well, it's pretty well assumed that you're going to go plot every data point. So I have a thousand rows, I get thousand points. If I have nulls, maybe it's half or null, maybe I end up with only 500 data points. It's only because things were null, it's not because there's some other selection mechanism. Um I guess for completeness, if you chose to facet that chart, maybe you split it on a categorical variable with ABC, you would then end up with three scatter charts where each one is receiving the subset of data that's relevant to it. And so depending on how you choose to do the faceting, you might do that as either a transformation coming into the scatter chart, or you might be able to do something internal to the scatter chart. You say, hey, it needs to fast it. All right, what else do we have? All right, so scatter chart, pretty simple. It's gonna fall them off. Next, think of our chart. R chart are where the complication starts. Because ultimately a bar is going to render single value per X value coming in to get this mapping of category to single value. Very often in Shifty, there was this assumption that bar charts are being used to show uh count aggregate. And so there was this internal transformation that would take place where it would find all the unique values, count them, display the count scaled to the max count or some other scale. And you might look at that and say, well, that's a sure assumption, it definitely is. And so realistically, if I'm calling that thing a bar chart, really it's like a bar count chart, or it's a bar visualization attached to a count transformation, and you're sort of doing it as a package deal. You can imagine with source data coming in, maybe the source data coming in is already the value you want to plot. Um so as long as there's no repeat. Actually, just render a value, I don't want to do the aggregation. Or yeah, I'm going to do the aggregation, but do it as a thumb. I've only got one item, but the equivalent is showing the value. Um, I guess it works. But that's a pretty uh convoluted way of just plotting a single value. And so this then it's kind of the first spot where you have to decide: do I want my interactive data visualization system to be strongly opinionated about the types of charts that we're going to create. And do I want to fake in a number of assumptions, in particular some of these common assumptions, that hey, a bar chart is really most useful if it's showing the count. And I think for an interactive data visualization, especially for the sort of stuff I would do with Shifty, it was absolutely the right choice, which is to say it was a completely defensible and good default. But where you get into trouble is in the default, it's the only option, and you've got people that might legitimately want to do something differently. So getting back to this idea that hey, my data already represents sales for the month. What I want to do is just plot them, sort them by max value, uh descending biggest to smallest, and then just give me a bar that's proportional to the value and give me a label that's equivalent to this cell. And yeah, I know there's duplicates in there, but it's fine. There's a reason there's duplicates, and I want them to come straight through. Even that is potentially invalid for certain types of visualizations. Because somebody might very well want to know. I don't know, maybe it's orders, maybe it's purchase price, and maybe they're wanting to understand total revenue by skew. Uh but there's a difference in the sale price, my customer, and so you end up with different rows or something. Not a particularly good example, but there might be some reason that you legitimately want to see the same value show up a couple different times. Um you might look at that and say, well, yeah, I guess we could do that, but that's uh that's just a that's just a poor chart selection. Like a bar chart is an objectively bad chart choice for what's being described there. Yeah, it is a pretty terrible choice. Um but let the user make the terrible charts that they wanted to make. Don't pin them in to make a terrible chart that your software only supports. And so right off the bat, we're catching a couple uh just defaults that existed in Shifty, defaults that are sane and reasonable if you're building interactive, especially dashboard type stuff, but those same defaults will absolutely get in the way of just building a robust Viz engine because having a baked-in assumption that every bar chart includes an aggregation and account means you're just guaranteed to produce bar charts that aren't actually what you want. So what does that mean? Well, it means what you really need to do is disentangle the visuals from the data transformations. And so even if you are building an interactive charting system, you're still just building some sort of bar chart visual that takes some incoming table, produces a scale, generates the glyphs, and then renders it all in the space that it's given. So for the interactive example, well, with the data it's being given from the Viz engine point of view, all it sees is the unique categories and the counts, and possibly the options or settings around scaling, coloring, etc. For the just hey, make me a bar chart example. What does it see there? Well, it just sees X values the Y values for the values to be run through the scale to create the bar, and then maybe some options for how to style the bars. It just exists to make the glyphs look correct. It exists to create the scales, maybe. Find the scales, maybe. The distinction there is you might have a global scale for how to convert a value into a coordinate. Maybe you've got three, four, five charts that are all meant to share a common x axis. And so in order for that to work, you need all the charts to render the same common limits. And so then for the first chart, well, let's say you want them to have common limits, but you still want them to auto-scale to the data. You look at that and say, ugh. So now I have to look at all of them as a group, produce my max, then get the limits, feed those limits into each individual chart, and then not let the data streams cross when we're actually doing the rendering. Also, it's very doable, but you've sort of got this paddle complication of I need to treat these charts as a group to get one number. The other way to do it is how I did it in Shifty. And this is to say, well, we'll cheat at the point in time where we need the X scale. We'll let React create a context. The context is rigged so that the first person in can create the scale. Every person that comes through can determine if they need to update the scale, if their max is bigger than its max. And so each time it renders, you're possibly updating the scale. And so this means for each of the groups, you will eventually arrive at the correct scale value. Now, granted, you've done a bunch of extra intermediate renders in between. It's not very performant, but it does produce the correct visual. Okay, but here we've got this uh here's here's where we're we're forced to introduce this strong separation between what is the table of numbers going into the visualizer, the thing that's actually producing the bars, and what's the table of numbers going into the thing the user has called the bar chart because depending on how they've configured it, they might view it as oh yes, this is a bar chart on a specific value. But from the bar charts point of view, it may actually be oh yes, I'm a dumb renderer, but upstream of me. The system has done an aggregation where they've shown me unique keys and counted the number of items. I think from like a UIUX point of view, if you're building interactive dashboards, you really probably want to provide a nice clean lake interface for hey, this is a bar chart that is automatically uh counting. And so it might just be this is a bar chart, what type is it? Uh it's a category counter type of bar chart. Is this oh it's just a value renderer. Maybe that's an explicit just toggle you have. Uh or maybe it's uh oh, this is an aggregator. The default aggregation is count. You can also do a sum. That is something I actually did and shift you early on. But what I found is people were more likely to get confused by it, especially if you are letting the aggregation update in real time as other stuff filters. And then especially if you're having this a sort in real time as stuff filters. And groups are coming and going, or the counts are coming or going. All of a sudden you get these charts that are flying all over the place. When I click this, these three charts are all going to update. If instead you're filtering by category, it's very reasonable. You can kind of like intuit in advance that all these counts are about to reduce by the number of bars that are now hidden. Something like that. Alright, next up. Basically, for each of the other chart types, they all kind of behave the same. And so you're sort of choosing uh what is the default transformation that's coming through? What are the actual glyphs being rendered and where do you do the computation if there's a transformation does it happen outside and then the renderer is dumb seven inside and then the render is much more of a rich kind of data viz component and honestly if you're just building a uh calculation and viz engine you would have these be fully separated from each other put the data stuff through the data stuff with the viz stuff through the biz stuff and then you just manage the handoff from on the max which is kind of opinionated default where this gets tricky though is the thing we're gonna talk about next which is let's imagine you're wanting to trace a row back to the place that it came from and so how you want to motivate this example um well let's take the example of a uh box plot because that's got a bit of stuff going on with it imagine have a box and whisker plot where you're choosing to plot uh quarter to third quarter interquartile range plotting the median plotting outliers and then you're uh doing a box per category. So you got a box per range, and you're showing uh average temperature or something, temperature uh with the category split by range for second, third, fourth, fifth, etc. Reverse neutral. And so you then want to understand how did this specific glyph get here. So I'm hovered on the circle. I want to right-click on it and then figure out what was the full sequence of transformations that made that thing show up right there. And so now you're starting to like unwind the full tree of logic. And this is one where I've got some handwritten notes that kind of describe what's going on. I actually implemented what I think is a reasonably working version of it, although this gets back to uh not really auditing it tightly enough to know if it's good or not. But the idea was that you have this complete uh data flow call chain for every single glyph, and so then every single item that's rendering you can walk back through the full chain of steps that need to happen in order for that glyph to get rendered. And so then if you wanted to understand uh why is that point right there? Oh, it's because we started with this source data, computed this scale value. You could basically click on the point and see all of that logic, including how the scale was determined, working all the way back to what was the source data. And then if the source data was the result of some aggregation or transformation, you could see kind of the group identity before and after. So you could identify that well, this aggregate group is coming from these data points over here. You can work your way all the way back to the individual rows of data that are influencing the calculation. And then you might find that the individual rows of data are actually showing up a couple reasons. Maybe the user has decided to compute the scale limits from some subset of data. And you can almost imagine a giant call graph where I guess it's sort of existed. It's like every step, at every step, you can see the inputs and the outputs. Some of the inputs and outputs will be rows of data. Rows of data coming in, rows of data coming out. Maybe it's a shelter, so it's one to one. You keep your key. And so the most you know is that this group coming out is speed by that column over there. Here's the possible ones that went in. Here's what came out. If it's an aggregation, a sum by this field, you know the field value. It's easy to see which rows are contributing to it. And so then I'm almost imagining a visual of like you've got a pivot table that pops out, and then maybe it's got four rows total. We can imagine off to the left is like four little table uh inset tables where each table is pointing to the row that it determines. And so your pivot table is grouping them by A, B, C, D. Then over on the left-hand side is you basically have split your source table into four pieces, one filtered by A, another by B, C, then D. And then you can imagine uh just clicking on one of those. Uh this is real similar to what Excel offers. If you right-click in the pivot table and say uh show details, maybe show data, whatever it's called, it'll give you a new table, which is the filtered values that show up in there. Basically the intersection. So that sort of thing can be really, really, really useful when you're doing diagnostics. It's a shame that Excel does it by uh modifying the source table, source hook, because that makes a complete mess. You're then inclined to delete it, but you know, don't make a mistake. Oh good. I think the other uh important piece of that uh call graph thing is it expresses the idea that every value that's being used somewhere could either be a constant, a default, some logic, some math, some calculation, some dynamic thing that came from somewhere else. And so then this call graph lets you see the as rendered state. Hey, here's how we got to this spot. And I think the importance of that is well the pivot table painted pretty clearly. And for a pivot table, it's kind of obvious what you're trying to do. I've got rows, I've got columns. They represent some subset of the data and then some column that's been processed. And so you could in all cases go show show the source data. Hey, here's a value of C. And imagine the value being plotted then as the sum of revenue. And so you do uh here's the rows of C, here's the revenues, here's the total value. You can confirm we got parity between that map and what's displayed. But then often in the pivot table, you're gonna do some logic like uh, well, show that as the percent of the grand total. You can say, okay, well, I know that value, but what's the grand total? Well, the grand total is the sum of all revenues across all cells. And you can show that over on the side. And now you've got this much longer column of numbers. They totalize to this value. You take the ratio, now you've got your percent of grand total. And you can do the same thing again and again for all the other iterations on it. Percent of row total, percent of column total, percent of blah blah blah blah blah. Each one has some logic whereby you're saying, hey, here's how we're determining the value. That's pretty much unchanging at all these. But pretty often that logic kind of uh outruns uh the ability of the person to analyze it. And so even if you've got this introspective ability, hey, here's the value came to be, uh, very often you're just hinting that it's probably a better visual. If every time you go to show it, you separately have to show this detailed calculation for how the values came to be. But who knows, it really varies by what people are trying to show. Or maybe here's a good example that actually depends that maybe what you're trying to do is catch an anomaly. You know that the day-to-day difference in total revenue should be basically zero. Uh, from day to day, it should be pretty much the same. And so instead of plotting average revenue and hoping you can swap the difference, you might instead do difference from previous. And then you could see the one day where instead of zero, we got this massive value. That's now that thing stands out like this worth on. Whereas it may have been hiding. If it said you're just wanting the raw values over and over again. But then as soon as you see the anomaly, you'd say, what's going on there? You might want to go pick the day and the day before and say, break that down for me. What happened there? I think you go get the column of values, which is the one day, the column of values, which is the next day. You show those totals, you take the difference, you see the outlier. And it's basically describing that you've got a sequence of steps to process the data. The data always ends up being some just addressable fields. And then you're going up producing visualizations off of those fields. And in some cases, you're baking defaults and constraints into the visuals. This bar chart is actually an aggregate counter sort of thing. But then in all these systems, you want to be able to keep track of what was the data that came in, what is the visual that came out, and for any given line or dot or anything. Show me exactly how it got there. And often it's I want to control that a little bit. Show me which knot you need to change to have control move, but all right, we'll stop there.